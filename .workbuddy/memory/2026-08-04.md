# 2026-08-04

## 合并提交 1df390c80 只读审查

- 审查了 `Merge test2 custom features into npcbots_3.3.5 base`，父提交为 `5c882898a` 与 `c331574c1`。
- 结论：合并不具备编译/发布条件；确认至少 7 个编译阻断问题和 6 组高风险功能回退。
- 主要问题涉及 Unit.cpp 初始化列表、MotionMaster/CreatureAI 花括号、InstanceScript 多余括号、botcommands 函数头、NPCBots 头文件声明缺失、IP 限制失效、LK IsNPlayer 回退、挑战模式钩子丢失、DK 宠物误解除。
- test2 的 16 个主要自定义模块目录完整保留；VIP、WhoList、辛达冰墓、卡拉赞象棋和 StatSystem 修复基本保留。
- 全面报告输出到 `outputs/merge_1df390c80_review_report.md`；未修改源码。

## 合并提交 1df390c80 数据库冲突只读审查

- 只读连接本地 MySQL 8.0.46，核对 `acore_auth`、`acore_characters`、`acore_world` 的 schema、固定 ID、模块数据及 `updates` 历史；未执行数据库写操作。
- 合并相对第一父提交涉及 31 个 SQL：1 个 core archive 修改、30 个模块 SQL；其中 25 个位于标准 `data/sql/db-*`，启用模块后会被 C++ updater 自动发现，本地无对应 MODULE 更新记录。
- 确认发布阻断冲突：zone difficulty 的 spell ID 主键冲突和 item 62000 覆盖、挑战表三套 schema 混用、raidleader 表破坏、AH bot 4608 行禁用列表及配置被重建、等级奖励覆盖、transmog/individual-xp 旧 command schema、transmog 旧 creature_template/spell_dbc schema、reward-shop 与 weapon-visual 的 SQL/C++ 错配。
- 确认 updater MySQL CLI 无 `--force`，首错停止但前序 DDL/DML通常已提交，SQL 不登记成功并会在后续启动重试，存在部分应用风险。
- 正式报告输出到 `outputs/merge_1df390c80_database_conflict_report.md`；结论为当前数据库不可直接运行全部模块自动更新，必须先备份、修正 migration 并在克隆库验证。
