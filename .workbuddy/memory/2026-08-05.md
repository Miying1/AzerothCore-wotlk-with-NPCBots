# 2026-08-05

## 合并提交 1df390c80 冲突重新处理

### 确定性编译阻断修复（7项）
- B-01 `Unit.cpp`: 删除 `DamageInfo` 构造函数中机械拼接的重复初始化列表和 `m_procEx`（基线已不收此类成员）
- B-02 `MotionMaster.cpp`: `MoveTakeoff` 补回缺失 `}` 闭合 `if(speed>0.01f)` 块
- B-03 `CreatureAI.cpp`: `_EnterEvadeMode` 中 formation 块补 `}`，TempSummon/EngagementOver 回到全局作用域
- B-04 `InstanceScript.cpp`: 删除文件尾多余 `}`
- B-05 `botcommands.cpp`: 补回丢失的 `HandleNpcBotWPSpawnAllCommand(ChatHandler*)` 函数头
- B-06 `bot_ai.h`: 补回 `SendEquipsToOwner()`、`SendEquipList(Player*)`、`LoadEquipPartName(uint8)` 声明
- B-07 `botdatamgr.h`、`botmgr.h`: 补回 `GetNpcBotCountByIp`、`SetBotName`、`GetIPMaxBots` 声明

### 行为冲突修复（7项，按用户确认）
- H-01 `botgiver.cpp`: 恢复 test2 的 IP 限制检查（GetNpcBotCountByIp + IpMaxBots + VIP豁免 + 绝对上限18）
- H-02 `boss_the_lich_king.cpp`: ShadowTrapLKTargetSelector、NonTankLKTargetSelector、VehicleCheck、CanAIAttack 四处 `IsPlayer()` → `IsNPlayer()`
- H-03 UtgardePinnacle/VioletHold/boss_skadi: 恢复 AddChallengeCreature/CheckChallengeMode/SetChallengeMode 钩子
- H-04 `bot_death_knight_ai.cpp`: 删除 test2 的 `JustSummoned` botPet 无条件解召，采用上游基线
- H-05 保留 `CheckOwnerExpiry` 中 `ownerLevel < 80` 不改
- H-06 `Spell.cpp`: `//ABORT()` → `ABORT()`
- M-01 `spell_item.cpp` 60510 双实现与上游一致，无需修改
- M-02 `boss_halion.cpp`: 硬编码 5384 → `SPELL_AURA_FEIGN_DEATH`

总计 16 文件变更，36 insertions / 14 deletions，全部通过格式检查。
