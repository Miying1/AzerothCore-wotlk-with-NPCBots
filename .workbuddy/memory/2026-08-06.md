# 2026-08-06

- 完成法师 NPCBot AI 战斗逻辑只读分析，覆盖 `bot_mage_ai.cpp`、`bot_ai.cpp/.h` 与 `botspell.h`；报告输出为 `outputs/mage_bot_ai_combat_analysis.md`。
- 确认法师 AI 采用“公共状态与生存/辅助优先，随后目标选择、控制/打断，最后输出”的固定优先级模型；整理了奥术、火焰、冰霜三系轮转和水元素、AOE、资源管理逻辑。
- 标记高可信度待核查项：火/冰结界初始化被注释、52 级思维冷却被动断档、正常冰枪轮转被注释、`oneOnOne` 判断不准确、结界错误依赖冰霜结界冷却；本次未修改战斗代码。
- 完成当前 `HEAD` 与权威基线 `upstream/npcbots_3.3.5` 的法师战斗逻辑差异分析，输出 `outputs/mage_bot_ai_baseline_diff_report.md`。确认 test2 主要改动包括奥术轮转重写、冰枪轮转关闭、冰霜新星门槛提高、跨专精技能收窄、唤醒提前、额外奥/火被动及水元素接触距离调整；高风险回归为低等级奥法填充缺失、火法火免目标寒冰箭兜底丢失、思维冷却 proc 条件被替换。
- 按用户要求将 `bot_mage_ai.cpp` 的霜火箭/火球瞬发条件同步回 upstream 基线：恢复燃烧环境、`BRAIN_FREEZE_BUFF` 和低等级无寒冰箭兜底判断，并恢复 `can_do_frost_or_fire`；`git diff --check` 通过，无冲突标记。注意该文件工作树中同时已有此前恢复冰枪轮转的改动，未在本次撤销。
- 为法师冰锥术增加全专精 PvP 近战贴身使用：遍历当前攻击者，若玩家控制单位正在攻击法师且进入近战范围，则法师转向该单位并优先自施放冰锥术；冰法原有锥形目标使用及火法龙息术逻辑保持不变。格式检查通过，无冲突标记。
- 按用户要求修改 `bot_rogue_ai.cpp` 的 PvP 战斗条件：以 `mytar->IsControlledByPlayer()` 作为 PvP/玩家控制目标判定；玩家控制目标时杀戮盛筵不再检查目标高血量门槛，刀扇不进入释放分支。仅修改代码，`git diff --check` 通过，未构建。
- 完成 `bot_paladin_ai.cpp` 相对 `upstream/npcbots_3.3.5@d914b8288` 的战斗逻辑差异分析，报告输出为 `outputs/paladin_bot_ai_baseline_diff_report.md`。确认主 `UpdateAI()`、`DoNormalAttack()`、治疗、嘲讽、祝福、光环和保护流程基本未重写；实质变化是防骑 20 级起获得命令圣印、45 级起重新启用 Combat Expertise，并新增防护坚韧/偏斜、防护与惩戒神圣之力、惩戒定罪等面板强化。标记 `49789`（DK Toughness）和 `16466`（Warrior Deflection）为跨职业法术 ID 风险，建议后续确认设计意图。该文件无工作树修改，`git diff --check` 通过，未构建。
- 完成 NPCBot Boss 战走位和团队战术的只读分析，报告输出为 `outputs/npcbot_boss_combat_movement_tactics_analysis.md`。确认当前架构为通用角色 AI、队形/AOE 走位、团队标记和少量 Boss 硬编码的组合，尚无统一 Encounter 状态机。重点问题包括鲜血熔炉英雄炸弹 ID 疑似误用普通 ID、远程带怪条件可能排除纯坦、安全点不检查移动路径、无安全点时可能回到仍处于 AOE 的主人，以及关键打断/嘲讽等缺少团队协调。建议先修明确缺陷，再引入路径危险检测、团队技能预约、换坦状态机和可扩展 Encounter 模块。本次未修改 Boss 战源码、未构建，报告 `git diff --check` 通过。