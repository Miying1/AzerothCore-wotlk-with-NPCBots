# 2026-08-07

- 完成 NPCBot 低侵入走位控制实施设计，输出 `outputs/npcbot_position_control_design.md`。
- 设计定稿：新增 `botpositioncontrol.h/.cpp`，由 `BotMgr` 持有独占控制器；新增 `mass`、`mass ranged`、`unmass`、`spread`、`spread off` 命令；不扩展 `BOT_COMMAND_*`、不改 SQL/RBAC/CMake。
- 核实关键接入点：`bot_ai::_calculatePos()`、`GetInPosition()`、`CalculateAttackPos()`、战斗更新循环和 `BotMgr::RemoveBot()` 生命周期路径。明确集合状态下必须同时抑制敌方追击并显式维持主人跟随，不能只在 `GetInPosition()` 早退。
- 方案规定 AoE、LOS、正面规避、坦克和副本强制走位优先于分散软约束；集合槽位稳定缓存，避免每帧随机抖动。此次只做设计文档，不配置、不编译、不修改核心源码。
- 最终一致性校验将控制器方案统一为 `BotMgr` 独占 `std::unique_ptr<BotPositionControl>`，避免静态全局注册表和跨主人状态串扰；文档补充了战斗中显式维持主人集合跟随的接入点。
