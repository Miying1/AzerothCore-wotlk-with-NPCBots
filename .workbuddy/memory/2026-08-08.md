# 2026-08-08

- 核查提交 `b94cf4fc83d195da616cc46c0b8a115503656448` 的局部撤销方案：该提交是当前分支祖先且非合并提交，涉及 `src` 下 92 个 C++ 源/头文件（新增 3、修改 89）。其中 4 个路径后续又有变化，普通反向补丁检查无法无冲突应用，建议使用仅针对 C++ 路径的三方反向补丁并人工处理冲突，避免直接恢复到父提交而覆盖后续修改。
- 用户确认 `Unit.cpp`、`InstanceScript.cpp`、`InstanceScript.h` 可直接覆盖为目标提交父版本；其余 C++ 文件仍宜通过反向补丁撤销以保留后续改动，已在后续删除的 `illidari_council.cpp` 应保持删除。
- 已创建保险分支 `backup-before-revert-b94cf4fc`（指向 `3f63f74fb5fae6308db287462e23339178b301af`），并将目标提交涉及且当前仍存在的 91 个 `src` C++ 源/头文件恢复到 `b94cf4fc^` 后暂存；验证无目标范围外暂存文件、无已跟踪的未暂存改动，且 91 个索引版本全部与父提交一致。未构建、未提交。
- 刷新并核查远程 `npcbots_3.3.5`：`origin` 的 `9f585ab6bfa61f2a8f680bbf25539e77c5c0aebc` 与 `upstream` 最新 `fd1e6b5ec038070da21720f3961103e4fe18b3c5` 中均存在 `commonPCH.h`、authserver `resource.h`、worldserver `resource.h`。
- 当前分支确定以 `upstream/npcbots_3.3.5` 为基准；已将上述 3 个文件恢复为上游 `fd1e6b5ec038070da21720f3961103e4fe18b3c5` 的精确版本。它们不再显示为变更，剩余暂存区为目标提交的 88 个 C++ 文件撤销（无范围外文件、无已跟踪未暂存改动）。
- 核查 `bot_ai.cpp`：最新上游 `fd1e6b5ec`、共同基点 `d914b8288` 及当前 `HEAD` 的文件 blob 均为 `638ed22d...`；上游自共同基点后未再改此文件。当前暂存/工作树 blob 为 `55aa63fe...`，等于 `b94cf4fc^`，相对上游有 297 行新增、82 行删除。这些差异全部来自当前分支在共同基点后的本地历史（含多个定制提交），不是遗漏的上游更新；因此从“上游最新 + 本地定制”角度已校准，但是否所有历史本地改动都仍属有意保留需业务确认。
