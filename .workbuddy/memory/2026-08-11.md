# 2026-08-11

- 详细补充 `outputs/海加尔山旧世界五人英雄裂隙传送设计.md` 的实施方案：明确旧世界副本使用普通 `InstanceMap` 作为场景外壳，T1/T2/T3 由裂隙运行状态而非客户端英雄难度表示；采用队长先传送、核心创建/选择实例、确认队长临时绑定后再传送队员的同实例流程。
- 新增 Boss 动态难度算法：有效参战值 `E=真实玩家×1.0+玩家NPCBot×0.7+Free Bot×0.8`，裂隙档位倍率叠加队伍规模倍率，开怪前动态调整、开怪后只升不降，并按 E 调整点名和场地目标数。
- 新增 Boss 房间锚点与安全环落点方案，要求高度、碰撞、距离和备用点校验；玩家 NPCBot 通过现有 `Player::TeleportTo` → `BotMgr::OnTeleportFar` 异步跟随，Boss 在 Bot 到齐前保持准备/不可攻击状态。
- 核对并记录重复挑战边界：普通副本临时绑定必须在队伍退出后安全重置，核心默认 `AccountInstancesPerHour=5` 仍生效；首期尊重该限制，不通过伪装 LFG 或全局关闭限制绕过。
- 修正裂隙队伍与难度规则（覆盖本日志前述动态缩放方案）：裂隙采用最多5个战斗席位，真实玩家与已入队玩家NPCBot共同占位，允许少人进入；Boss强度、目标数和机制只由固定Boss模板与T1/T2/T3决定，不再按玩家/Bot数量动态调整，也不使用有效参战值E。
- 修正NPCBot进本规则：裂隙脚本只主动传送真实玩家，不直接传送或重定位Bot；跟随主人且已经在队伍中的玩家NPCBot通过现有 `BotMgr::OnTeleportFar` 流程自动进入主人所在副本，Free Bot不进入。文档已清除人数缩放公式及主动Bot传送表述，并通过17章、472行、代码块配对与禁用术语校验。
- 更新裂隙重复挑战规则：裂隙玩法自身不设置日锁、周锁、Boss锁定、次数或冷却，允许队伍反复连续进入；计划将服务器 `AccountInstancesPerHour` 调整为50，作为滚动一小时窗口内的核心安全保护上限。每场结束后仍必须等待成员退出并安全重置实例、清理本场临时绑定，下一场创建新的 `instanceId`；达到50次时明确提示并恢复未成功消耗的裂隙。
- 核查“伪装LFG队伍”方案：仅设置 `isLFGGroup()` 不足以获得实例计数豁免，核心要求同时是LFG组且 `IsLfgRandomInstance()`；完整伪装会引入LFG地下城状态、难度设置、数据库状态、随机本冷却、传送/解绑、投票踢人及NPCBot地下城补位等耦合。裂隙继续采用普通队伍与队长实例绑定，NPCBot由 `BotMgr::OnTeleportFar` 自动跟随更简单、可控。
- 重构裂隙代码组织设计：统一使用独立脚本模块 `src/server/scripts/rift/`，根目录保存共享定义、配置、管理器、传送、启动上下文、运行组件、Boss公共基类和总加载入口；每个旧副本建立独立子目录，实例适配、本副本共享定义、注册聚合文件和每个Boss的独立脚本均置于子目录。确认当前CMake会自动扫描一级脚本目录并递归收集源码，小写目录 `rift` 的模块入口按现有规则为 `AddriftScripts()`。仅更新设计文档，未创建源码或构建。
- 按“五人英雄裂隙”的明确语义进一步修订模块命名：源码目录改为 `src/server/scripts/HeroicDungeonRift/`，模块入口改为 `AddHeroicDungeonRiftScripts()`；共享类型同步改为 `HeroicDungeonRiftTier`、`HeroicDungeonRiftRunState`、`HeroicDungeonRiftLaunchContext` 和 `HeroicDungeonRiftRunComponent`。随后按最新约定将普通源码文件及 `AddSC_` 注册函数前缀简化为 `rift_`，Boss脚本前缀使用 `boss_rift_`；设计文档已清除旧目录和 `heroic_dungeon_rift_` 文件前缀，未创建实际源码或构建。
- 根据用户最新需求扩展五人英雄裂隙设计：入口从裂隙解耦为通用入口服务，支持NPC按T1-T3选择后由服务端随机副本和Boss；Boss源坐标继续采用现有副本Boss坐标，玩家唯一传入点改为配置数据，由用户后续提供并按1-2码随机散点；新增配置表设计保存mapID、源Entry/GUID、传入点和T1-T3倍率/新Entry；新增实例内动态出口门，按runToken+玩家GUID返回各自原位置，避免覆盖Player全局Recall点。
- 生成 `outputs/五人英雄裂隙数据库配置与Boss模板SQL（暂不执行）.sql`：包含5张自定义配置表、Entry从100000开始的三档模板生成形态、模型/法术/抗性/Addon复制模板和独立掉落说明；所有新模板INSERT均置于注释块内，当前未执行、未导入数据库。
- 按最新简化要求重构五人英雄裂隙配置：实例运行态只放服务器内存；最终静态配置简化为 `heroic_dungeon_rift_boss` 与 `heroic_dungeon_rift_boss_tier` 两表；T1/T2/T3新Entry统一使用“每个Boss一份、三Tier共用”的同一C++脚本，由Tier分支控制强度和技能；统一出口门Entry规划为100500，固定对话“离开...”。随后将所有裂隙设计合并为唯一文档 `outputs/五人英雄裂隙最终设计文档.md`，删除旧的重复裂隙设计文档和旧五表SQL，仅保留简化版SQL草案；同步修正海加尔主文档中的交叉引用。最终离场规则改为只记录发起入口对话者进入前的位置作为 `sharedReturnLocation`，其他队员不记录各自返回点，所有通过出口门离开的真实玩家都返回该同一位置。未执行SQL、未创建源码或构建服务器。
