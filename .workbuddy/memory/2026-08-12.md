# 2026-08-12

- 按用户最新分配完成五人英雄裂隙 Entry 重排：入口 NPC 固定为100000；16个Boss的48个Tier模板连续使用100001–100048；裂隙专用同伴/召唤物保持100100–100110；T1/T2/T3出口门保持100500/100501/100502。统一审核SQL `outputs/五人英雄裂隙全部副本整合SQL（暂不执行）.sql` 已同步并通过静态唯一性校验，SQL未执行。
- 完成统一SQL一致性审查：核对16个Boss各3个Tier、11个专用召唤物、28个SQL ScriptName与C++注册、入口来源模板8379及模型7591、当前World数据库表字段、83级生命反向折算和三档出口模型。入口100000会先清理旧模板的法术、抗性、Addon、装备和移动附属数据，避免早期草案残留。
- 修复裂隙战斗逻辑：`BossAIBase::JustDied`统一清理记录的裂隙召唤物，覆盖瑟玛普拉格炸弹等无独立死亡清理的Boss；莫格莱尼增加复活阶段标记，复活后不再重复假死，可正常真死；莫格莱尼和怀特迈恩任一方真死会同步结束另一方。
- 复核召唤物倍率：普通武器伤害通过实例武器属性缩放，法术伤害通过召唤物AI的非DIRECT_DAMAGE回调缩放，不对同一种伤害重复叠乘；Hallshaper治疗API、召唤物上限和清理逻辑均通过编译审查。
- 使用 Visual Studio 2026 Developer PowerShell 初始化MSVC环境后完成最新22步Ninja增量构建，全部裂隙Boss脚本编译成功，生成 `scripts.lib` 并链接 `build-ninja/bin/worldserver.exe`。未修改核心源码。
- 更新唯一最终设计文档，写入8个地图、16个遭遇、召唤物增强、最新Entry区间和构建结果；将死亡矿井首批SQL及简化配置SQL明确标记为已被全部副本整合SQL取代。
- 补全 `outputs/boss传入点.xls` 的经典旧世五人副本首领清单：按现有副本名称整体跳过已有行，新增怒焰裂谷、哀嚎洞穴、影牙城堡、暴风城监狱、血色墓地/图书馆、祖尔法拉克、玛拉顿、黑石深渊、黑石塔下层、厄运之槌三翼、通灵学院、斯坦索姆正后门共115行；数据来自 `instance_encounters` 和 `creature_template_locale` 的 zhCN 名称，新增行 `x/y/z/o` 均为空，最终129条数据且无重复 boss_entry_id。
- 核实五人英雄裂隙现有16个遭遇的Boss生成坐标来源：最终整合SQL中的 `boss_x/y/z/o` 是从基线 `data/sql/base/db_world/creature.sql` 对应原版Boss静态刷怪行提取并固化到 `heroic_dungeon_rift_boss_tier`；运行时不查询原版 `creature` 表或复制原Boss当前位置，而是读取配置缓存并通过 `SummonCreature(run->EntryId, run->BossSpawn, ...)` 动态生成。
- 按原版 SmartAI 链路改造 `src/server/scripts/HeroicDungeonRift/scarlet_monastery/boss_rift_mograine_whitemane.cpp`：怀特迈恩改从原版静态位置 `1202.13, 1399.07, 29.0931, 3.12414` 动态出现，通知后打开104600号房门并通过 `MovePoint`/`MovementInform` 移动到莫格莱尼；50%血量触发深度睡眠并移动到莫格莱尼，抵达后等待4.5秒施放血色复活，复活命中后才进入第二阶段。第一阶段限制为惩击/护盾，第二阶段再启用治疗/精神控制；保留双方死亡同步和裂隙召唤物清理。未配置或构建，已完成格式、行宽和API静态审查。
- 对齐裂隙血色大教堂双首领喊话：确认裂隙 Entry（莫格莱尼100028/100029/100030，怀特迈恩100100）无法直接命中原版 `creature_text` 的3976/3977记录，因此在 `boss_rift_mograine_whitemane.cpp` 硬编码官方 `zhCN` 文本，并复用原版语音音效5835–5840。已接入莫格莱尼开战/击杀/复活后、怀特迈恩出场/击杀/复活喊话，未新增SQL；完成行宽、Tab、尾随空白和 `git diff --check` 静态检查，未重新构建。
- 使用 MySQL MCP 连接本地 `acore_world`，完成 `outputs/英雄裂隙boss传入点.xls` 的 `boss_xyz` 填充：117/117 行均为 `x,y,z mapid` 格式。107行来自 `creature` 静态默认刷新点；10个事件Boss通过数据库 SmartAI/变身链及当前副本脚本固定召唤点补齐（穆坦努斯、加兹瑞拉、耐克鲁姆、塞瑟斯、秩序竞技场、乌洛克、基兹鲁尔、基尔图诺斯、巴纳扎尔、拉姆斯登）。复合3976/3977行取首个Boss莫格莱尼3976的位置；最终核验主表118行9列、117个坐标均非空且格式有效。
- 完成16个英雄裂隙主Boss的“进入即生成、初始可攻击”静态复核：`rift_manager.cpp` 首位成功入图成员即可触发动态生成，不再要求队长先进入；`BossAIBase::Reset()` 统一恢复可见、站立、主动，并清除不可攻击、不可选中、玩家/NPC免疫、假死和隐藏身体标志；整合SQL对主Boss清零 `unit_flags2`/`flags_extra`、清除Addon站姿低字节且不复制主Boss Rooted移动限制。原版阿扎达斯石化唤醒、伊兰尼库斯/迦玛兰免疫移除、阿库麦尔四火/门禁等副本前置不会作用于独立裂隙Entry；怀特迈恩出场、莫格莱尼假死、迦玛兰地缚图腾及其他召唤波均属于战斗内机制。`git diff HEAD --check`通过；另核对16个源模板Addon，只有范克里夫的Thrash（3417）和拉姆塔斯的Battle Stance（7165）常驻光环，均非前置/锁定状态；未执行SQL，未重新构建。
