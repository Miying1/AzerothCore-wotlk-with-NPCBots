# 2026-08-13

- 完成 Sheet2 全部 31 个新五人英雄裂隙 Boss 的代码实现与构建：按副本建独立子目录（wailing_caverns/shadowfang_keep/zulfarrak/maraudon/blackrock_depths/blackrock_spire/dire_maul/scholomance/stratholme，杜安加入既有 scarlet_monastery），boss_id 17–47、Entry 100200–100292、新召唤物 100300–100312；扩展 rift_defines.h/rift_manager.cpp/rift_script_loader.cpp，新增共享 RiftSummonAI 基类；整合 SQL 追加 93 条 Boss + 13 条召唤物映射，DELETE 范围扩至 1–47，主 Boss 83 级、召唤物 82 级。MSVC+Ninja 增量构建通过，worldserver.exe 链接成功。
- 按注意点复查并修正 31 个新 Boss 代码：所有硬编码喊话改为 zhCN 中文；每个 Boss/NPC 加中文注释（Boss 中文名 + 法术中文名），并标注 T2/T3 新增技能。
- 查漏补缺：补齐 6 个缺 T2 的 Boss（mutanus/gahzrilla/roccor/baelgar/smolderweb/ravenian）与 doan/argelmach/thaurissan/immolthar/ukorz 的 T2 或 T3；修正 2 处错误法术 ID（rivendare 死亡缠绕 17466→6789，roccor 11802 黑铁地雷→13880 熔岩喷溅，smolderweb 20482 不存在→12023 蛛网）；springvale 补回原版圣盾术（<30%无敌）；清理 ras_frostwhisper/vyletongue/celebras/arugal/arugal_voidwalker 中未使用的法术与事件定义。构建通过。
- 七贤还原为7名成员：主Boss末日之链(9039)+仇恨者/愤怒者/邪恶者/忧郁者/沸腾者/愚昧者6名同伴（新召唤物 100313–100318，各自原版技能）；SQL source_entry 9035→9039。还原阿鲁高随机传送(7136/7586/7587)、加丁暗影传送门6房间+门开关+复生守卫、阿格曼奇4构造体(8900/8906/8907/8920→100319–100322)。T2/T3新增技能统一：读条用 triggered=true 不可打断、冲锋/点名选随机目标、冲锋带打断(法术反制/盾击)用 SelectCastingPlayer 优先打断读条目标（BossAIBase 新增该辅助）。构建通过。
- 排查构建环境：确认 amd64 MSBuild.exe 启动即崩溃(0xC0000005)，x86 版正常、64 位 PowerShell 正常，故 VS 解决方案生成器(.slnx)不可用；改用 Ninja + VS2026 MSVC 重新配置 `build-ninja/` 成功（PowerShell 执行 Launch-VsDevShell.ps1 设 vcvars 环境后 `cmake -S src -B build-ninja`）。原 `build/` 目录（VS 解决方案）已清空废弃。构建要求：C++20/MSVC≥19.24（实际 19.51）、CMake 3.16–3.22（实际 4.3.1 可用）、MySQL 8.0.46、Boost 1.91.0、SDK 10.0.22621.0。
- 重新配置 build-ninja（迁移后 build-ninja 已删）：`Launch-VsDevShell.ps1` 非交互模式卡 `Read-Host` 失败、PowerShell 工具又禁用 cmd.exe（不能用 `cmd /c vcvars64.bat && ...`），改用手动注入 MSVC/SDK 环境 + cmake 4.4.2 显式完整路径 + 补 MySQL 路径后配置成功（Configuring 4.8s + Generating 1.2s，build.ninja 12.7MB 已生成）。可复用命令（PowerShell）：
  ```powershell
  $msvc="D:\Program Files\Microsoft Visual Studio\18\Community\VC\Tools\MSVC\14.51.36231"
  $sdk="C:\Program Files (x86)\Windows Kits\10"; $ver="10.0.22621.0"
  $env:PATH="$msvc\bin\Hostx64\x64;$sdk\bin\$ver\x64;$env:PATH"
  $env:INCLUDE="$msvc\include;$sdk\Include\$ver\ucrt;$sdk\Include\$ver\shared;$sdk\Include\$ver\um;$sdk\Include\$ver\winrt"
  $env:LIB="$msvc\lib\x64;$sdk\Lib\$ver\ucrt\x64;$sdk\Lib\$ver\um\x64"
  & "C:\Program Files\CMake\bin\cmake.exe" -S "E:\workbuddy\AzerothCore-wotlk-with-NPCBots" -B "E:\workbuddy\AzerothCore-wotlk-with-NPCBots\build-ninja" -G Ninja "-DCMAKE_MAKE_PROGRAM=D:\Program Files\Microsoft Visual Studio\18\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\Ninja\ninja.exe" "-DCMAKE_C_COMPILER=$msvc\bin\Hostx64\x64\cl.exe" "-DCMAKE_CXX_COMPILER=$msvc\bin\Hostx64\x64\cl.exe" -DSCRIPTS=static -DMODULES=static "-DMYSQL_INCLUDE_DIR=D:\Program Files (x86)\mysql\include" "-DMYSQL_LIBRARY=D:\Program Files (x86)\mysql\lib\libmysql.lib"
  ```
- cmake-gui 从零配置易踩两个坑（本次用户复现）：① generator 默认选 "Visual Studio 18 2026" 会生成 `.slnx`，而本机 amd64 MSBuild 崩溃、.slnx 不可用，必须改选 Ninja；② CMake 内置 FindMySQL 在 Windows 上不会自动定位 MySQL，`MYSQL_INCLUDE_DIR`/`MYSQL_LIBRARY` 会 NOTFOUND，须手动设（`D:\Program Files (x86)\mysql\include` / `...\lib\libmysql.lib`）。故建议直接复用已配好的 `build-ninja/`（缓存已含 Ninja+cl.exe+MySQL 三项），不要用 cmake-gui 从零配、更别用 `build/` 目录。
- 用户坚持用 cmake-gui + "Visual Studio 18 2026" generator，故新增 `CMakePresets.json`（项目根），内含 `win-vs2026-x64` configurePreset：generator=Visual Studio 18 2026、architecture=x64、binaryDir=build、预置 `MYSQL_INCLUDE_DIR`/`MYSQL_LIBRARY`/`SCRIPTS=static`/`MODULES=static`，并带同名 buildPreset(RelWithDebInfo)。已用 `cmake --preset win-vs2026-x64` 验证通过（Configuring 4.7s + Generating 2.1s，生成 `build/AzerothCore.slnx`，MySQL 不再 NOTFOUND）。cmake-gui 打开项目后顶部下拉选该 preset 即可。注意：VS 生成器配置能成功，但真正编译仍需 MSBuild，若 amd64 MSBuild 崩溃则只能回退 Ninja。
- 用户反馈 amd64 MSBuild 崩溃问题已修复（此前 0xC0000005 启动即崩溃的约束不再成立）。据此 VS 2026 生成器方案已可用（`build/AzerothCore.slnx` 就绪），Ninja 保留为备选。MEMORY.md 构建环境约束已同步更新。
- 生成本地 Excel 整合文件 `outputs/英雄裂隙boss传入点_整合.xlsx`：Sheet1「经典旧世」整合原 sheet1+sheet2 共 45 个经典旧世 Boss，Sheet2「燃烧的远征(70级)」含 15 个 TBC 五人本共 51 个 Boss。数据来源为本地 MySQL acore_world（凭据 root/wow@80；默认 acore/acore、root/password 均被拒）。列：地图/map_id/BOSS/boss_entry_id/boss_xyz/x/y/z/o/备注。原始刷新点取自 creature 表 position_x/y/z/orientation；脚本召唤类 Boss（经典 3：穆坦努斯3654、加兹瑞拉7273、巴纳扎尔10813；TBC 6：瓦兹鲁登17537、安苏23035、德雷克17848、斯卡洛克17862、时空猎手18096、预言者斯克瑞斯20912）无静态刷新点，备注「脚本召唤」；血卫士伯鲁恩20923 为英雄模式专属。注意：该 .xls 实为 xlsx(ZIP) 格式但扩展名 .xls，openpyxl 因扩展名拒绝，须复制为 .xlsx 或传 file-like；本机隔离 Python 在 `C:/Users/Administrator/.workbuddy/binaries/python/envs/default`（含 xlrd/xlutils/openpyxl/pandas/xlsxwriter），DBC 表 map_dbc/mapdifficulty_dbc/dungeonencounter_dbc 在库中为空，故副本-Boss 映射靠手工枚举 entry。
- 追加「玩家传入点」列：在整合文件新增 transfer_xyz(传入点"x,y,z") 与 transfer_o(传入面向) 两列。经典旧世 45 个 Boss 的传入点取自源表(sheet1 的 x,y,z,o / sheet2 的 xyz,o)；70级非召唤 Boss 为空(源表无)；脚本召唤 Boss 从原版 C++ 脚本/SAI 取召唤点：瓦兹鲁登17537=(-1406.5,1746.5,81.2,5.46)、安苏23035=(-87.61,287.84,26.5,2.33)(SAI smart_scripts target_type=8)、德雷克17848=(2172.76,149.54,87.981,4.19)(instancePositions[2])、斯卡洛克17862=(1995.78,277.46,66.64,0.74)、时空猎手18096=(2640.49,696.15,64.31,4.51)、预言者斯克瑞斯20912=(445.763,-191.639,44.64,1.60)。

- 检查 47 个裂隙 Boss 脚本三项指标（法术数据用 outputs/Spell_dbc.csv + 本地 acore_world）：①T2/T3 新增读条技能全部用 `CastIfConfigured(...,true)`（triggered=true），经 `UnitAI::DoCast→Spell::prepare` 使 m_casttime=0 变瞬发，"读条不可打断"注释与实际不符（有读条的技能：闪电箭15801/毒液喷吐15664/荆棘诅咒16247/冰冻术18763/烈焰风暴12468/暗影箭20791/恐惧12096/支配心灵14515/纠缠根须12747 均被抹掉读条）；②近战基线 DamageModifier=35 与纳克萨玛斯 10 人 18 Boss 全量一致（✅），法术伤害用 ×15 启发式需实测；③T1 血量严重不达标：SQL 反向折算 HealthModifier 使 83 级模板生命=源 Boss 原始生命（2,165~50,300），而目标=纳克萨玛斯 10 人平均 2,864,923 的 6/10≈172 万，差 34~794 倍。已生成报告 `outputs/五人英雄裂隙BOSS脚本检查报告.md`。纳克萨玛斯 10 人 18 Boss entry：15928/15931/15932/15936/15952/15953/15954/15956/15989/15990/16011/16028/16060/16061/16062/16063/16064/16065。

- 按用户要求修改裂隙 BOSS 实现并写入设计文档规则：①新增技能改为"免疫打断"——BossAIBase::Reset 施加 `me->ApplySpellImmune(0, IMMUNITY_EFFECT, SPELL_EFFECT_INTERRUPT_CAST, true)`，读条类 T2/T3 技能 triggered=true→false（去掉 true 恢复读条），闪电箭15801 保持 triggered=true 顺发，注释统一改"免疫打断/顺发/瞬发"（脚本批量改 55 处，含七贤枚举行修正）；②施法时间覆盖用 spell_dbc（不改 Spell.dbc 文件）：烈焰风暴12468 3s→2s(CastingTimeIndex 5)、冰冻术18763 3.5s→1s(CTI 4)，覆盖行用 CSV 按 SpellEntryfmt 位置映射生成全 234 列、只回填 enUS/zhCN 名称；③T1 血量基线改正向：`HealthModifier = 1800000 / basehp2(83, unit_class)`（整合SQL 已改，召唤物仍×10 保持原折算）。SpellCastTimes.dbc 关键值：1=0(瞬发)/4=1000/5=2000/14=3000/16=1500/22=3500。设计文档 6.3/6.4/第10节已同步（新增 6.4 免疫打断+施法时间规则、6.5 脚本命名）。注意：spell_dbc 覆盖是全局的（12468 另有4模板+3SmartAI、18763 各1处），且仅回填 enUS/zhCN 名称。未构建（AGENTS.md 默认不构建）。

- 再次调整裂隙 BOSS 规则并写入设计文档：①新增技能按施法时间分档——>1.5s 一律顺发(triggered=true)，≤1.5s 保留读条+免疫打断；去掉 BossAIBase::Reset 的全局免疫打断，改为基类 `AddInterruptImmuneSpell()` + `OnSpellStart/OnSpellCast` 按技能 ID 临时开关 `ApplySpellImmune(SPELL_EFFECT_INTERRUPT_CAST)`（ras 恐惧12096、mutanus/verdan 纠缠根须12747 注册），T1 基础技能恢复与源 Boss 一致可打断；10 个 >1.5s 技能(毒液喷吐/荆棘诅咒/冰冻术/烈焰风暴×2/暗影箭×4/支配心灵)改回 triggered=true 顺发。②召唤物血量改为按比例：`_rift_summon_map` 加 boss_source_entry 列，召唤物 HealthModifier = 180万×(源召唤物原始生命/源Boss原始生命)/basehp2(82)（已验证：怀特迈恩160万/七贤成员130-169万/小怪2千-50万，比例合理）。③设计文档规则改为"以后新增技能尽量顺发，否则≤1s"。④删除不再需要的 spell_dbc 施法时间覆盖(第9节)。⚠️ 发现整合SQL 引用了不存在的表 `creature_template_model`（本 fork 用 creature_template.modelid1-4 存模型），会导致 SQL 执行失败，属预先存在的 bug 待修。

- 纠正上一条误报：以 `data/sql/base/db_world/` 基础表为准核对，整合SQL 引用的 `creature_template_model` 是**正确的**（基础库确有该表，字段 CreatureID/Idx/CreatureDisplayID/DisplayScale/Probability/VerifiedBuild，属新版 AzerothCore：creature_template 无 modelid 字段、模型独立成表）。整合SQL 所有表引用与字段（creature_template_model/spell/resistance/addon/equip_template/movement、creature_template、gameobject_template）均与基础库匹配，无需修改。误报根因：实际数据库 acore_world 是**旧版 schema**（creature_template.modelid1-4，无 creature_template_model），与基础 SQL 不一致——若按 acore_world 部署，整合SQL 会在 creature_template_model 处失败，实际库需按基础 SQL 重建/迁移。

- T1 血量改为按职业分档（方案A，用户确认）：HealthModifier 固定值——战士/圣骑士(unit_class 1/2)=129→血量≈180万、法系(unit_class 8)=115→血量≈128万（血量=basehp2(83)×HealthModifier，随职业 basehp2 自然浮动）。47 个源 Boss 中 unit_class=8 仅「秘法师杜安」1 个，其余 46 个为 class 1/2。整合SQL 用 `@RIFT_BOSS_HM_MELEE=129`/`@RIFT_BOSS_HM_CASTER=115` 变量，召唤物血量跟随所属 Boss 新血量(按职业)×(源召唤物/源Boss比例)（额外 join bossDst=83级 basehp2）。已实测验证：战士/圣骑士 1798905、法师 1282940、怀特迈恩 1599027。设计文档 6.3 节与第10节公式已同步。
- 完成70级裂隙静态收尾：22个Boss文件/47个AI/14个loader与SQL ScriptName完全对应；SQL共69组207条Tier、59条召唤物，70级66条Tier传入点全NULL且基础配置禁用。新增 `RiftLevel70SummonAI`，25类70级召唤物只应用Tier倍率和damageCoefficient，不继承经典召唤物的统一15倍法术基线，60级行为不变；依据Spell_dbc确认TBC多项召唤物法术已有千级基础伤害。凯尔萨斯重力失效增加正常结束、死亡、脱战、Reset四路径光环清理。设计文档和实现注意点已同步。未执行SQL，未执行本轮构建；`git diff --check`通过。
- 最终复核70级裂隙静态交付：`var/validate_70_rift_sql.py` 六项检查全部通过（Boss ID 1–69、Boss Entry 100001–100207、召唤物Entry 102000–102058连续；70级承接48–69/100142–100207/102034–102058；两份SQL均使用基数变量、偏移表达式和DamageModifier变量；70级66条Tier坐标NULL、`dungeon_version=70`、`enabled=0`；SQL/C++ 59个召唤物Entry及AI注册一致，无旧Entry残留）。`git diff --check`退出码0，仅提示两个交付文件未来CRLF转LF；HeroicDungeonRift下114个C++文件无Tab或尾随空格。四个交付文件确认存在并可审核；仍未执行SQL、未构建、未链接或进服测试。
- 完成60级47个Boss的T2/T3新增技能逐Spell DBC伤害校准：普通直伤/周期伤害使用独立Tier1基准，混合效果只覆盖真实伤害BP，武器/百分比/控制/治疗/召唤/增益保留原机制。特殊链增加裂隙专用AuraScript：杜安15790周期父Aura接管15791/BP0（1800/跳T1，保持非triggered引导及定向免打断）；蕾瑟塔蒂丝16247 Proc父Aura接管16248/BP0（3500 T1，以Boss为originalCaster走DamageDealt）；塞雷布拉斯21793覆盖BP0=1800/跳；摩尔达11972保留BP0打断并覆盖BP1=3500。完整整合SQL增加两个spell_script_names绑定，设计文档与实现注意点同步。全量只读复核未发现其他确认遗漏；`git diff --check`和SQL六项验证通过。未构建、未执行SQL或进服测试；全仓codestyle仍被NPCBots/核心历史问题阻塞。
