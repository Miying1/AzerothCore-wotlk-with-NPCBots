# 2026-08-14

- 按用户最终要求统一逐技能Tier伤害语义：删除临时辅助 `GetTierScaledRaidDamage()`；`GetUnscaledRaidDamage()` 改为只反算Boss原版 `_raidSpellDamageMultiplier`，不反算Tier倍率，所有 `CastFinalRaidDamageSpell()` 参数统一解释为T1基准伤害，再由 `DamageDealt()` 应用T1/T2/T3的1.0/1.4/2.0倍率。审查17个文件26处调用，无手动Tier预乘或旧方法残留。布洛戈克酸息术基准1500/跳+3000直伤，对应T2约2100/跳+4200、T3约3000/跳+6000；摩摩尔音速尖啸30000改名并明确为T1基准总量，T3约60000总量。设计文档和实现注意点已同步；静态验证六项及 `git diff --check` 通过，未构建、未执行SQL。
- 分析本地 `acore_world.item_template` 的高等级装备成长：确认 `ItemLevel>=213` 共4979条，排除测试/自定义异常后以4968条史诗武器/护甲（213–284）拟合。得到指数外推公式 `目标值=基准值*exp(k*装等差)`；物理单手/双手DPS每+13装等约×1.1010/×1.1032，常规基础护甲约×1.0384。整理245级物理/法系武器和布皮锁板护甲原版锚点，交付中文指南、公式化Excel设计器、拟合参数CSV与245样本CSV到 `outputs/`。核心确认普通装备数值由 `item_template` 直接存储，设计时应复制同类245模板并在总预算内重分配属性，而非逐条机械同比放大。
- 基于上述拟合与本地史诗装备模板生成245装等完整装备库，共161件、Entry `910000–910160`（生成前数据库占用0）：覆盖布/皮/锁/板8个护甲槽、项链/戒指/饰品/披风、15类实用武器、法系副手/盾牌和圣契/神像/图腾/魔印，并包含法系输出、力量输出、敏捷输出、治疗、坦克五类定位。绿字先按 `k=0.00733` 缩放至245，再用固定种子33524508逐条施加±8%随机（实际0.9202–1.0795）；DPS与护甲使用分类拟合系数。外观全部复用本地紫色或更高品质同类型displayid，武器优先知名传奇，护甲核心五件优先ICC T10。交付SQL、CSV清单和中文说明至`outputs/`；SQL未执行。静态校验、覆盖校验及`git diff --check`通过，官方SQL检查器因GitHub不可达未能运行。
- 按用户要求审计60级47个裂隙Boss原版技能时间轴：46个脚本存在原版技能通过 `ScheduleTieredEvent`、`TierDelay` 或 `_tier` 分支随难度缩短CD，现已将Boss本体及原版同伴/阶段守卫/召唤物的首次施法与循环CD统一固定为原T1档（仅T3/非T3分支取非T3值）；明确T2/T3新增技能与仅高Tier启用的新机制继续保留难度CD。已同步 `outputs/五人英雄裂隙最终设计文档.md` 与 `outputs/英雄裂隙BOSS实现注意点.txt`；静态搜索确认60级原版事件无分层调度残留，`git diff --check`通过，未构建、未执行SQL。
- 按用户新约束重新读取已更新的本地 `acore_world.item_template` 结构，确认当前138列且已移除 `StatsCount`。基于新结构生成577件240装等PVE装备，Entry `920000–920576`（当前占用0），排除饰品与圣契/神像/图腾/魔印；每件至少2种白字和2种绿字且不含韧性。属性组合与数值现仅参考本地装等>=213史诗PVE装备：统计确认700/700件精神样本同时有耐力，527件坦克样本中227件为防御+威胁混合；生成结果126件精神装备均有耐力，59件坦克装备中42件采用防御搭配命中/精准。绿字使用种子33524008做±10%浮动（实际0.9001–1.0998）。外观来源限定蓝/紫/橙品质且 `RequiredLevel=1–79`，28个橙武displayid全部唯一；名称全局唯一且不含布甲/皮甲/锁甲/板甲。SQL按当前138列完整显式列名生成，577行均逐行解析为138值，未执行；交付SQL、CSV清单和中文说明至`outputs/`。
- 复核240装等生成值与设计指南的一致性：旧版虽应用单项属性公式，但以各项中位数拼装，577件中86件属性条数高于基准，且3件投掷武器误用弓弩枪DPS系数，因此判定整件预算不合格。随后按用户要求彻底重构为完整模板预算：每件绑定一件213–284级史诗PVE完整模板，按各属性独立拟合k归一至240，保持模板属性条数和预算结构，归一完成后才对绿字逐条施加±15%（种子33524008，实际0.8502–1.1497）。577/577件属性条数与模板一致、无额外属性条；565件精确完整模板，9件力量单手武器使用同子类坦克模板并将防御评级槽位等价映射为输出评级，3件力量长柄复用同手位力量双手完整预算，映射均写入CSV。投掷武器改用独立k=0.00723926。最终SQL为当前138列、577行各138值，Entry 920000–920576仍占用0，未执行；目录/预算/SQL解析及git diff检查全部通过。
- 在240装备库中新增独立猫德8件、熊T8件武器线，覆盖双手锤/长柄/法杖，总数更新为593件、Entry `920000–920592`（数据库占用0）。源码核实 `ItemTemplate::getFeralBonus()` 使用 `max(0, floor(DPS*14)-767)`，普通玩��和NPCBots均由武器DPS获得野性AP；专用武器逐件记录DPS、攻速和2673–2693 FAP。猫德模板保留敏捷/耐力/AP及输出评级；熊T偏高耐力并禁用防御、招架、格挡、格挡值和破甲。所有野性武器使用213–284完整物理双手模板，不继承触发法术。修正回退装备为“先按原词条k归一，再映射评级类型”，强化逐项预算、FAP精确值、CSV/SQL行数交叉验证。最终593/593模板条数保留，绿字0.8502–1.1497，每行138值，28个橙武displayid唯一；SQL未执行。设计说明已重写为最终指南，保留最终系数、预算/回退/野性机制、外观和上线验证，移除旧随机流程与过程性重复信息。
- 全量补齐60级47个裂隙Boss脚本注释：Events/Spells增加中文名称及原版/T1、T2、T3归属，补充组合战、原版同伴、阶段守卫、召唤数量与存活上限、混合BP及父Aura触发子法术说明。纠正弗莱拉斯燃烧之灵Tier归属和裂隙版死亡爆炸说明、贝尔加80/60/40/20%阈值、7399实际为恐吓及“顺发”错字。最终复核覆盖47文件、62个事件枚举和237个事件项；无超120字符注释行，`git diff --check`通过。本轮只改注释，未构建、未执行SQL。
- 按用户后续要求将593件240装备的最终绿字独立浮动由±15%改为±10%（种子33524008，实际0.9001–1.0998），并以独立种子33524010分配宝石插槽：按四舍五入选415件带孔（69.98%），其中125件双孔（占带孔30.12%）、290件单孔，另178件无孔。插槽仅使用红2/黄4/蓝8，第三孔、全部socketContent及socketBonus均为0，避免额外插槽奖励预算；CSV增加插槽数量/颜色/Bonus审计列，最终指南增加插槽规则。复审发现整数舍入可能造成最终值相对归一值越过±10%边界，已修正生成器为将最终整数值限制在ceil(归一值×0.90)至floor(归一值×1.10)，并令CSV记录实际生效倍率；SQL解析验证增加CSV与socket字段逐字段交叉核对。最终验证通过：593行各138值，0/1/2孔=178/290/125，严格绿字倍率0.90–1.10，SQL未执行。
- 按用户命名约束重构全部593件装备名称：名称不含任何数字，每件最多出现一次“·”；最终593个名称全部唯一、均恰好使用一个“·”，最长11字符。生成器、CSV验证和SQL解析器均新增命名约束及SQL/CSV逐行名称交叉校验；重新生成SQL、清单和指南后，完整模板预算、±10%、插槽分布及593行×138列验证继续通过，SQL未执行。
- 按等级拆分裂隙SQL：`outputs/60级五人英雄裂隙SQL（暂不执行）.sql` 已删除全部70级Boss与70级召唤物映射、相关偏移变量和跨等级写入逻辑，仅保留boss_id 1–47、Boss Entry 100001–100141、召唤物102000–102033；`outputs/70级五人英雄裂隙扩展SQL（暂不执行）.sql` 已从整合表格“燃烧的远征(70级)”工作表回填22个遭遇的玩家X/Y/Z/O，基础表与Tier表均写入坐标并设enabled=1。维斯路登/纳杉按表格中的传令官瓦兹德行沿用地狱火城墙传入点。静态验证确认60级141条Tier/34召唤物、70级66条Tier/25召唤物范围连续，70级无NULL坐标且66条Tier逐项匹配表格；SQL未执行。
- 基于最终240设计规则建立独立250装备流程，不覆盖240文件：生成577件250史诗PVE装备，Entry `930000–930576`（数据库占用0），定位仅法系输出167、治疗164、敏捷输出117、力量输出70、坦克59，不含猫德/熊T专用行。基准严格213–284完整模板，577/577保持属性条数；逐项归一至250后绿字严格±10%整数边界（0.90–1.10），回退统计565/9/3。插槽按240规则为0/1/2孔173/283/121，404件带孔（70.02%），双孔占带孔29.95%，Content/Bonus清零。采用独立命名前缀，577名称唯一、无数字、最多一个“·”，与240名称重叠0。最终SQL 577行各138值、28个橙武displayid唯一，SQL未执行；并输出独立CSV和250设计说明。
