# 2026-08-15

- 为240/250/260三档PVE装备生成器增加独立的 `reference_loot_template` 等概率组，并将SQL追加进对应的 `item_template.sql`：Reference Entry分别为924000、934000、944000。组内统一使用 `Chance=0`、`GroupId=1`、`LootMode=1`、数量1，分别覆盖593、577、577件装备。
- 重新生成三份SQL并验证：item_template仍分别为593/577/577行且每行138列；reference掉落行分别为593/577/577，所有字段一致且本地数据库目标Reference Entry当前未占用。SQL未执行。
- 更新三个SQL解析验证器，使其只解析首个item_template INSERT，兼容同一文件随后附加其他表的INSERT语句。
- 只读核查 `outputs/60级五人英雄裂隙SQL（暂不执行）.sql` 与 `outputs/70级五人英雄裂隙扩展SQL（暂不执行）.sql` 对本地 MySQL 8.0.46 / acore_world 新接口的兼容性：AzerothCore 核心表字段、列数、源模板、模型、83/82级 classlevelstats、Entry 范围及132个 SQL/C++ ScriptName 均匹配；本地裂隙基础表仍为旧9列，缺少 C++ 强制查询的 `dungeon_version`。随后修复60级SQL的1064错误：将 MySQL 8.0不支持的 `ADD COLUMN IF NOT EXISTS` 改为查询 `information_schema.COLUMNS` 后通过 PREPARE/EXECUTE 条件执行普通 `ADD COLUMN`，本地 MySQL 8.0.46 只解析验证通过；完整裂隙SQL仍未执行。建议继续给6组附属表的无列名 `INSERT ... SELECT` 补显式列名。
- 按更新后的 `outputs/英雄裂隙boss传入点_整合.xlsx` 同步4个曾与Boss点重合的玩家传入点：沃尔丹5775→`-138.47,-4.46,-28.17,0.48`、塞雷布拉斯12225→`796.76,62.35,-86.86,2.75`、阿格曼奇8983→`801.99,22.66,-53.66,6.12`，写入60级SQL各3个Tier；摩摩尔18708→`-156.64,-392.15,17.08,4.72`，写入70级SQL各3个Tier。逐项与Excel一致，距Boss分别67.54/72.36/45.26/105.19米；Boss生成点未改，SQL未执行。
