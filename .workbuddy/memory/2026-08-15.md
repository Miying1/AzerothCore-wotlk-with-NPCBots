# 2026-08-15

- 为240/250/260三档PVE装备生成器增加独立的 `reference_loot_template` 等概率组，并将SQL追加进对应的 `item_template.sql`：Reference Entry分别为924000、934000、944000。组内统一使用 `Chance=0`、`GroupId=1`、`LootMode=1`、数量1，分别覆盖593、577、577件装备。
- 重新生成三份SQL并验证：item_template仍分别为593/577/577行且每行138列；reference掉落行分别为593/577/577，所有字段一致且本地数据库目标Reference Entry当前未占用。SQL未执行。
- 更新三个SQL解析验证器，使其只解析首个item_template INSERT，兼容同一文件随后附加其他表的INSERT语句。
- 只读核查 `outputs/60级五人英雄裂隙SQL（暂不执行）.sql` 与 `outputs/70级五人英雄裂隙扩展SQL（暂不执行）.sql` 对本地 MySQL 8.0.46 / acore_world 新接口的兼容性：AzerothCore 核心表字段、列数、源模板、模型、83/82级 classlevelstats、Entry 范围及132个 SQL/C++ ScriptName 均匹配；本地裂隙基础表仍为旧9列，缺少 C++ 强制查询的 `dungeon_version`。随后修复60级SQL的1064错误：将 MySQL 8.0不支持的 `ADD COLUMN IF NOT EXISTS` 改为查询 `information_schema.COLUMNS` 后通过 PREPARE/EXECUTE 条件执行普通 `ADD COLUMN`，本地 MySQL 8.0.46 只解析验证通过；完整裂隙SQL仍未执行。建议继续给6组附属表的无列名 `INSERT ... SELECT` 补显式列名。
- 按更新后的 `outputs/英雄裂隙boss传入点_整合.xlsx` 同步4个曾与Boss点重合的玩家传入点：沃尔丹5775→`-138.47,-4.46,-28.17,0.48`、塞雷布拉斯12225→`796.76,62.35,-86.86,2.75`、阿格曼奇8983→`801.99,22.66,-53.66,6.12`，写入60级SQL各3个Tier；摩摩尔18708→`-156.64,-392.15,17.08,4.72`，写入70级SQL各3个Tier。逐项与Excel一致，距Boss分别67.54/72.36/45.26/105.19米；Boss生成点未改，SQL未执行。
- 修复五人英雄裂隙启动时将全部地下城坐标误判为无效的问题：`rift_hooks.cpp` 改为在 `OnBeforeWorldInitialized()` 加载 `ConfigStore`，不再使用早于 DBC、`instance_template` 和 `creature_template` 的 `OnLoadCustomDatabaseTable()`。`git diff --check` 通过；全仓 C++ 风格检查仍被大量既有、与本改动无关的问题阻断。当前本机 `acore_world` 的两张裂隙表均为空且 boss 表仍是缺少 `dungeon_version` 的旧结构，与用户日志对应的运行库状态不一致。
- 将240/250/260装备Entry改为连续区间并在各SQL顶部声明 `@item_id_start`：240为100000–100592、250为100593–101169、260为101170–101746；`item_template` 与 `reference_loot_template.Item` 的INSERT均使用 `@item_id_start + 偏移`。同步重生成三档清单及Item.dbc CSV，确认目标Entry区间在本地数据库未占用，三个SQL验证器均通过，SQL未执行。
- 定位并修复裂隙Boss脱战后不重置且永久无法攻击：2026-08-08合入的挑战难度代码在 `CreatureAI::_EnterEvadeMode()` 已提前设置 `UNIT_STATE_EVADE` 后再次用 `IsInEvadeMode()` 返回 `false`，导致外层回家移动与 `Reset()` 永不执行、EVADE状态无法清除。移除该矛盾的后置判断，保留挑战光环刷新和编队回避通知；无需修改 `BossAIBase`。
- 严格沿用装备设计指南生成230装等标准PVE装备577件（不含猫德/熊T专线），Entry 99423–99999、Reference Entry 914000；产出138列 `item_template.sql`、装备审计清单、增量 `Item.dbc.csv`、合并基础 `Item.csv` 的完整WDBC二进制 `Item.dbc`及设计说明，SQL未执行。三层校验及二进制DBC结构校验全部通过：护甲384/通用70/副手8/盾12/武器103，带孔404（双孔121），绿字倍率0.9–1.1，数据库目标Entry与Reference Entry均未占用。
- 为 `npc_rift_entry` 增加“指定T1/指定T2/指定T3”三个 coded Gossip 选项：严格解析输入的 `boss_id`，通过 `ConfigStore::GetTier(boss_id, tier)` 校验已加载且启用的对应难度配置，并复用统一的建实例、运行状态、整队绑定和传送流程；原随机T1/T2/T3入口行为保持不变。修改 `rift_entry.cpp`、`rift_defines.h`、`rift_manager.cpp`，`git diff --check` 通过；未按项目约定主动构建，全仓风格检查仅被既有的非裂隙代码问题阻断。
- 新增 `data/sql/updates/pending_db_world/rev_20260815_01.sql`：Entry 93003 物品“指位技能”使用 `spellid_1=483/spelltrigger_1=0` 与 `spellid_2=90019/spelltrigger_2=6`，点击学习法术90019；同步写入zhCN名称和说明。事务内导入校验通过并回滚，未实际修改本地数据库。
- 为全部69个裂隙Boss接入Spell ID级独立伤害调谐：新增 `rift_spell_damage.h`，覆盖200个Boss脚本声明的伤害Spell ID，固定/AOE/DoT/控制附伤分别设置T1基线，武器技能保留DBC结算并使用独立武器倍率；`BossAIBase`统一路由 `CastIfConfigured/DoCast/DoCastVictim`，T2/T3仍仅沿用 `TierConfig::DamageMultiplier`。同步移除卡加斯刃舞和拉伊奥术毁灭的临时Boss级倍率切换，并取消杜安奥术飞弹、蕾瑟塔蒂丝荆棘反伤的旧15倍反算。静态审计69脚本/200伤害Spell全覆盖，`git diff --check`通过；未完成构建验证（曾误启动scripts目标后立即终止）。
- 将召唤物伤害并入同一套Spell ID调谐：`RiftSummonAI`统一覆盖固定伤害、DoT和武器技能，继续叠加召唤物Tier与damageCoefficient，并仅对未调谐触发子法术保留经典副本旧基准；改造阿库麦增援、行走炸弹、怀特迈恩、迦玛兰图腾、奥达曼守卫及希尔拉克焦点火焰等特殊路径，移除召唤物旧`*15`补偿。拉伊植物裸Spell ID改为命名常量后纳入表。最终审计69个Boss文件/112个裂隙C++文件/202个伤害Spell ID，其中42个召唤物伤害Spell ID，`missing=[]`、`missing_summon=[]`；`git diff --check`通过，按约定未构建。
- 修复血色大教堂莫格莱尼/怀特迈恩流程：原版怀特迈恩是预置单位，莫格莱尼降至1点生命后通知她开门、喊话并移动到尸体旁；此前裂隙版只在致死瞬间临时召唤，存在召唤失败后静默导致“怀特迈恩不刷”。现在莫格莱尼Reset时预先准备并保存怀特迈恩GUID，致死时执行原版式假死与启动动作；怀特迈恩50%进入深度睡眠，靠近莫格莱尼后4.5秒施放血色复活，SpellHit后双方进入第二阶段。新增重置动作、可见性/战斗状态恢复和失败日志；静态关键节点检查及`git diff --check`通过，未构建。
