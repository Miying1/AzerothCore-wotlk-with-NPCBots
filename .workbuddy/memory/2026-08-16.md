# 2026-08-16

- 修改250/260装等PVE装备生成器：为103件武器按定位从触发法术效果表分类池随机选择1个触发法术，写入 `spellid_1`，统一 `spelltrigger_1=1`；力量输出池为攻击强度类，敏捷输出池为物理急速/攻击强度类，法系输出池为法术强度类，治疗池为治疗急速/法术强度类，坦克池为护甲/减伤类。非武器保持所有触发法术字段清零。
- 重新生成 `outputs/WotLK335_250装等PVE装备_item_template.sql` 和 `outputs/WotLK335_260装等PVE装备_item_template.sql`，同步更新两份设计说明。两档各577条、138列，武器各103件；解析校验器均通过（250/260 `errors=[]`），生成器重建验证确认所有武器法术均属于对应定位候选池、`spelltrigger_1=1`、spellid_2..5全为0。SQL未执行。
- 用户提供的 `outputs/触发法术效果.xlsx` 只有一个工作表、22条候选记录；本地编辑器读取接口中途断开，改用只读XLSX XML解析完成读取，未修改该表。
- 修改 `outputs/60级五人英雄裂隙SQL（暂不执行）.sql` 与 `outputs/70级五人英雄裂隙扩展SQL（暂不执行）.sql`：每个裂隙Boss Tier的 `creature_template.lootid` 设置为自身Entry，并写入 `creature_loot_template` Reference掉落绑定。T1为230装等2槽+240装等1槽，T2为250装等2槽，T3为260装等2槽；复用Reference池914000/924000/934000/944000，60级覆盖141个Tier Entry、70级覆盖66个Tier Entry。SQL未执行，`git diff --check`与静态范围/数量校验通过。
- 修复裂隙T1已调谐技能实战被放大的路径：Boss侧不再应用旧 `SetRaidSpellDamageMultiplier` 倍率，召唤物侧始终移除历史60级15倍法术基准；伤害法术优先使用最近一次实际施法ID，避免 `GetCurrentSpell()` 读到旧技能。保留T2/T3的 `TierConfig::DamageMultiplier` 和武器技能独立倍率。审计69个Boss文件/112个裂隙C++文件/202个伤害Spell ID/42个召唤物伤害Spell ID，missing均为空；未构建。
- 按用户要求，将本轮伤害缩放实现新增的英文说明注释统一改为简体中文，并同步修改伤害表生成器中的表头注释，避免重新生成后恢复英文；`git diff --check`通过。
- 确认裂隙T1独立伤害仍被放大的引擎层原因：`SpellEffectInfo::CalcValue()` 会在自定义BasePoints写入后，对非玩家控制的83级生物施放、带 `SPELL_ATTR0_SCALES_WITH_CREATURE_LEVEL`、SpellLevel不同且无RealPointsPerLevel的伤害效果继续乘 `BaseDamage[83]/BaseDamage[SpellLevel]`。class1经典内容20级法术系数为 `49.2375/9.6823=5.08531`；当前202个调谐法术中静态识别到59个法术（63个效果）满足该缩放路径。此前移除旧15倍只消除了后置重复倍率，未绕过此处的前置引擎缩放。
- 已实现裂隙专用 `CalcValue()` 等级缩放反向补偿：在自定义BasePoints写入前，按与引擎一致的施法者控制状态、SpellLevel、RealPointsPerLevel、缩放属性、效果/Aura类型和触发子法术条件，使用对应unit class与creature expansion的 `spellPower/casterPower` 预缩小。统一调谐入口、`CastRaidTunedSpell`、两种 `CastFinalRaidDamageSpell`、行走炸弹、杜安奥术飞弹及荆棘反伤特殊路径均已覆盖；不修改全局SpellInfo属性，T2/T3与武器倍率不变。数值闭环中20级7000基线经预补偿1377后回到约7002.5（仅整数舍入误差）；伤害表审计missing均为空、`git diff --check`通过。全库C++风格检查仍被大量既有NPCBots等文件问题阻断，本轮修改文件未出现对应新增告警；按约定未构建。
