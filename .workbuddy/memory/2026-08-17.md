- 继续核对 NPCBot 下线/上线问题调用链：确认 BOT_REMOVE_LOGOUT=0；LogoutPlayer 调用 RemoveAllBots(0)，RemoveBot 最终执行 ResetBotAI(BOTAI_RESET_LOGOUT)，不会清空 characters_npcbot.owner，但会从 BotMgr::_bots 移除并清除 creator/master 运行时绑定。
- 确认 BotMgr::Initialize 仅在 NPCBot 系统启动时加载全局 BotData；BotMgr::LoadData 只加载玩家设置；BotMgr::Update 在 HaveBot() 为 false 时直接返回，不会自动 AddBot/RebindBot。
- 确认 bot_ai::GlobalUpdate -> FindMaster 只在 NPCBot AI 自身已经被全局更新时尝试找主人；但 BotDataMgr::Update 仅处理不在世界且 FindMap 有效、IsFreeBot 的实体，且玩家 BotMgr 不会因为此路径自动恢复 _bots 绑定。因此登录后不存在可靠的自动重绑路径。
- 确认无主人时 ResetBotAI/TeleportHomeStart -> TeleportHome -> GetHomePosition，普通 NPCBot 直接读取 creature 表的 map/position，故最终回出生点；手动 .npcbot command rebind 只对 BOT_COMMAND_UNBIND 状态生效，而登出路径没有设置该标志。
- 针对“只有一个 Bot 留在原地且无法交互”的新现象，定位到更具体的状态机原因：Logout 的 ResetBotAI(BOTAI_RESET_LOGOUT) 在 Bot 仍处于 world 时不会调用 TeleportHomeStart，而是仅将 _atHome=false、spawned=false；BotMgr::RemoveBot 也只在无地图/副本/临时 Bot 时 RemoveFromWorld，因此普通世界地图 Bot 可能留在原地。
- bot_ai::FindMaster() 有前置条件：me->IsInWorld() && (!_atHome || _evadeMode) 时直接 return；因此该 Bot 位于原地且 _atHome=false 时，玩家上线即使在身边也不会自动重绑 master。GlobalUpdate 的 gossip 恢复同样要求 (!_evadeMode && _atHome)，所以 NPC_FLAGS_GOSSIP 不会恢复，表现为玩家无法对话。
- 该单 Bot 症状比“统一登录未恢复”更符合一个卡在 _atHome=false / teleport 状态机的实例；还需现场确认其 IsDuringTeleport、command state、NpcFlags、owner、map/坐标和 AI 日志。未修改源码、SQL，未构建。
- 新增关键证据：卡死 Bot 的 Buff 不倒计时，说明不仅是跟随/主人状态异常，而是该 Creature 没有持续进入 Creature::Update/Unit::Update；Creature::Update 在 bot_AI->canUpdate=false 时直接 return，连 Unit::_UpdateSpells 都不会执行。BotMgr::Update 只会对已绑定 _bots 调用 bot->Update，且 HaveBot() 为空直接 return；登录后未重绑的 Bot 因此可停在 world 但完全冻结。
- 分析崩溃转储文本 `E:\tmp\0498e7a1f795+_worldserver.exe_[17-8_9-38-9].txt`：确认是 worldserver 在 `BotMgr::UpdatePvPForBots()` 第1373行调用 `bot->SetByteValue(UNIT_FIELD_BYTES_2, 1, ...)` 时，于 `Object::SetByteValue()` 第753行访问非法对象内存（C0000005）。转储中的 bot/owner 对象字段已明显损坏；重点怀疑 NPCBot 生命周期/跨线程并发与 `_bots` 中悬空 `Creature*`，而非 `SetByteValue` 本身或数据库错误。建议先修复迭代过滤和对象生命周期，再用 PageHeap/ASan/调试器复现验证。