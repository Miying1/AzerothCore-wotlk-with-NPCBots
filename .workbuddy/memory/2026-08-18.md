- 分析玩家下线后 NPCBot `_atHome` 不恢复为 true 的路径：`ResetBotAI(BOTAI_RESET_LOGOUT)` 本身不直接设 true；只有后续 `Evade()` 正常执行并满足非 Wanderer、非战斗、无受控/施法/移动阻塞、回到 home 附近等条件时才会在 `bot_ai.cpp:18854` 设 true。下线时若 bot 仍在传送状态，`UpdateAI` 会在 `IsDuringTeleport()` 处短路；`CleanupsBeforeBotDelete` 只有 bot 不在世界时才 AbortTeleport。`HideSpawns=true` 且无 GM、延迟传送回调持续被取消/覆盖、bot 不再 UpdateAI 或为 Wanderer 等情况可导致长期不恢复。