- 修复"午夜冲锋秒人"：冲锋实为骑乘版阿图门(16152)的 Charge 29847，DBC 显示是武器伤害型（Effect2=58 武器伤害+74，非纯控制），随 DmgMod=77 缩放后伤害≈177.07×77+74≈13,710，选 8-25 码远程目标，对布甲暴击≈27,000 秒杀。
- 修复：新增 spell_karazhan_attumen_charge 脚本（SpellScript OnHit SetHitDamage 重写为固定 urand(8000,9000)），注册到 AddSC_karazhan_spell_scaling，SQL 单独绑定 29847→spell_karazhan_attumen_charge（独立脚本名，不混入 spell_karazhan_direct_scale）。
- 关键教训：判断"控制类技能"时不能只看技能名/文档标注，要查 DBC Effect 字段——冲锋 29847 是 Effect96(位移)+Effect58(武器伤害)+Effect64(触发34719近战急速)，含武器伤害成分，之前 §5.1 误标为[控]遗漏。武器型技能随 DamageModifier 缩放，升级 DmgMod 会放大这类"打远程"技能的伤害。
- 撤销 29711「击倒」的缩放：29711 是午夜(Midnight)的技能，DBC 显示 Effect1=2 直伤(固定1500, base1499+die1) + Effect2=6 光环 ApplyAura=12(SPELL_AURA_MOD_STUN 昏迷/击倒控制)，无触发、无武器伤害成分。本质是击倒控制技能+象征性1500固定小伤，之前误按"控制+小伤"缩放3.0×(1500→4500)，违背"控制技能不缩放"原则。
- 已从三处移除：C++ 倍率表、技能伤害缩放SQL.sql、设计文档(代码块+SQL块+§6.1表格)，并在 §8 不缩放清单新增"控制|午夜·击倒|29711|昏迷控制(附带1500固定小伤)"条目。缩放列表 75→74 个法术，C++与SQL校验一致。
- 更正：撤销 29711「击倒」的做法已回退。用户指出项目里并无"控制技能不缩放"原则——"不缩放"的真实依据是技能"无伤害"（缩放无意义）、武器型走 DmgMod、百分比/机制型锁血等，而非"控制"标签。29711 击倒有 Effect1=2 直伤(固定1500)，属"有伤害"，应纳入缩放。
- 已恢复 29711 为 3.0×（1500→4500），回到 C++ 倍率表、技能伤害缩放SQL.sql、设计文档(代码块+SQL块+§6.1表格)，并从 §8 不缩放清单移除"午夜·击倒"条目。缩放列表 74→75 个法术，C++与SQL一致。
- 经验教训：给技能定"不缩放"时，必须依据 DBC Effect 字段判断"是否有伤害"（Effect2直伤/Effect6+周期伤害/Effect58武器/Effect64触发），不能按"控制/减益"这种语义标签一刀切。类似地，之前冲锋 29847 也是因漏看 Effect58 武器伤害成分才误判为[控]。
- 删除 spell_karazhan_attumen_charge 冲锋重写脚本：移除 C++ 类定义+RegisterSpellScript 注册+#include "Random.h"（urand 仅此脚本使用）、SQL 中 29847 的 DELETE/INSERT 绑定、设计文档 §8 该条目改为"武器型，走 DmgMod"。
- 后果：冲锋 29847 恢复原生武器伤害型，随 DmgMod(77) 缩放 ≈13,710，会再次对布甲秒杀（此前重写为固定 8000-9000 正是为防秒人）。C++ 倍率表仍 75 个法术，与 SQL 一致。
- 发现贞洁圣女"圣洁之地"(Holy Ground)=29523 在 Spell.dbc 里缺失（29520-29528 区间跳号 29523），脚本 boss_maiden_of_virtue.cpp:35 的 DoCastAOE(29523,true) 静默失败，技能无效、无伤害。全 src 仅此一处引用，SpellInfoCorrections 无补正。属"断链引用"（同 29767/29857 类）。
- 复核盲区教训：C++ BossAI 脚本里硬编码的 DoCastAOE/DoCast spell ID 不进 smart_scripts 表，之前"从 smart_scripts 拉施法记录"的复核方式覆盖不到；须额外核对 C++ Boss 脚本的 SPELL_* 枚举。
- 整理卡拉赞各 Boss 的 lootid（查 acore_world.creature_template.lootid + creature_loot_template 校验），并更新设计文档《卡拉赞80级10人升级设计文档.md》三处表格（§执行摘要 Boss 目标参数表、§4.3 可选 Boss、§4.4 歌剧院）各加 LootID 列。关键发现：阿图门掉落挂骑乘版 16152（非骑乘 15550 lootid=0）、午夜 16151 无独立掉落、歌剧院 Oz 四单位(17535/17546/17543/17547) lootid=0 掉落集中在老巫婆 18168；其余 Boss LootID=自身 Entry。
- 生成卡拉赞 Boss 新装备掉落绑定 SQL（outputs/卡拉赞/卡拉赞Boss装备掉落绑定SQL（依赖装备SQL先执行）.sql）。机制确认：reference_loot_template 装备池编号 230=914000/240=924000/250=934000/260=944000/270=954000（generate_wotlk*.py 生成，池内装备 GroupId=1+Chance=0 等概率选1件）。掉落：250=阿图门16152/莫罗斯15687/贞洁圣女16457 各2件+剧院4单位(老巫婆18168/大灰狼17521/罗密欧17533/朱丽叶17534)各1件；260=馆长15691/泰雷斯蒂安15688/埃兰之影16524/虚空幽龙15689 各2件；270=玛克扎尔王子15690/夜之魇17225/泰恩里斯28194 各2件。关键坑：这些 Boss 原掉落已占用 GroupId 1~3，新槽须用 GroupId 10 避免互斥。装备 item_template 与 reference 池均尚未导入数据库。
- 更正掉落槽方案：不用"掉N件=N个GroupId槽"，改为"单槽(GroupId=10)+MinCount=MaxCount=N 控制件数"。源码 LootMgr.cpp 约束 reference 记录 MinCount 必须==MaxCount（否则 ReportInvalidCount 报错），MaxCount 决定 reference 池 Process 次数（每次从池内等概率选1件）。最终 14 条记录：10 主线 Boss MinCount=MaxCount=2，剧院 4 单位 MinCount=MaxCount=1。
- 卡拉赞象棋事件：给麦迪文的回响(16816)的 gossip 增加"开门!没空陪你下棋,你个老6"跳过选项（boss_chess_event.cpp）。新增 enum MEDIVH_GOSSIP_SKIP_CHESS(=4)，sGossipHello 无条件追加该菜单项，sGossipSelect 新增 case：清理棋盘(REINIT_PIECES+_deadCount.fill) + 移除 GAME_IN_SESSION + SetData(DATA_CHESS_EVENT, DONE) 打开通往王子的门(184277)。需重编译 worldserver 生效。
- 清除卡拉赞 Boss 原版 Reference 掉落：查库确认 14 个 Boss 中 10 个挂原版 T4 装备池(reference 34016~34023/34031/4113)，已 DELETE creature_loot_template 里这 10 条 reference 条目（Entry 16152/15687/16457/15688/16524/15689/17225/18168/17521/28194，验证后这 14 Boss 的 Reference>0 计数=0）。王子15690/馆长15691/罗密欧17533/朱丽叶17534 无 reference（用直接 Item 掉落），未动。生成 outputs/卡拉赞/卡拉赞Boss原版Reference掉落清除SQL.sql（含回滚 INSERT 注释块）。注：这些 reference 条目 Item 字段与 Reference 字段同值(如34017/34017)，引擎判断 reference 只看 Reference 字段(IsValid 的 `if(!reference)` 分支)；清除后 34016 等池成孤儿，启动会打印 "isn't referenced and thus useless" 警告，不影响功能。
- 将清除原版 Reference 掉落的 DELETE 合并进 outputs/卡拉赞/卡拉赞Boss装备掉落绑定SQL（依赖装备SQL先执行）.sql（按用户要求未执行数据库）。合并后执行顺序：SET 变量 → 清除旧 250/260/270 绑定 → 清除原版 34016~34023/34031/4113(10条) → INSERT 新装备 → 审核查询；头注释"不改原掉落"改为"清除原版掉落"。
- 更新 230/240/250/260 装等 PVE 装备名称（共 2324 件），风格从固定"秘星·星纱X洞察/迅捷/破阵/无瑕"改为 270 装等的魔兽自由命名（地域/称号/意象/动作/装备称谓混合、非固定句式）。生成器 var/rename_equip_230_260.py（固定种子 20260821），词库按槽位/武器subclass/定位风味分类；只改 item_template.sql 的 name 字段和清单.csv 的"名称"列，其余字段 byte 级不变。校验通过：全局唯一、与 270/280 不冲突、无数字/禁用词/多次"·"。备份在 outputs/装备生成/backup_名称更新前/，对照表 230-260装备名称更新对照表.csv。教训：Python re 里 `^(@...)` 的 `(` 是分组符非字面量，要匹配 SQL 行的字面 `(` 须用 `\(`，否则静默不匹配导致替换失败。
- 分析 mod-ah-bot 模块（modules/mod-ah-bot/，AzerothCore 官方拍卖行机器人），产出 outputs/mod-ah-bot模块功能与配置说明报告.md。核心：卖家(Sell)/买家(Buy)两独立开关；三阵营 AH 各一 AHBConfig（2联盟/6部落/7中立）；物品经 InitializeBins 过滤后入 14 个品质分箱（品质×贸易/普通）；卖家按 minprice~maxprice% 定一口价、minbidprice~maxbidprice% 定起拍价；买家心理价=供应商价×buyerprice 倍率；可选市场价机制(MarketResetThreshold=25 重置统计)。关键坑：Account/GUID 必须指向真实账号角色(否则启动禁用)，14 档占比须=100，黑名单与白名单同时为空会主动关卖家。配置 conf/mod_ahbot.conf.dist + world 库 mod_auctionhousebot/mod_auctionhousebot_disabled_items/auctionhousebot_professionItems 三表；GM 命令 ahbotoptions(SEC_GAMEMASTER)。
- 设计 mod-ah-bot 上架规则改造方案（outputs/mod-ah-bot上架规则改造设计方案.md + mod-ah-bot上架规则表SQL.sql）。核心发现：现有 SellerWhiteList 白名单在"来源过滤"(默认只放行掉落)之后才生效，配了非掉落物品(如附魔卷轴)会被提前丢弃，白名单名不副实；且白名单只能精确 entry，无法表达"分类+等级"。方案：新增 world 表 mod_auctionhousebot_seller_rules（item_id 精确 / item_class+subclass+等级/品质区间 分类规则），表空=原行为、表非空=规则模式(命中直接分箱、旁路选择性过滤)。改动点：Config.h 加 SellerRule 结构+vector+MatchSellerRules、Config.cpp InitializeFromSql 读表、抽 Classify() 分箱函数、InitializeBins 循环开头插入规则判定、底部检查适配。物品分类速查：宝石 class=3、附魔卷轴 class=0 subclass=6(ITEM_ENHANCEMENT)、附魔材料 class=7 subclass=12、附魔配方 class=9 subclass=8；品质枚举 0灰1白2绿3蓝4紫5橙6黄。附带发现 bug：AuctionHouseBot.DisablePermEnchant 在 InitializeFromFile 从未 GetOption 读取(配置项无效)。
- 扩展上架规则：加"指定价格(金币)"+"上架比例"两维度。规则表增 buyout_price_gold/bid_price_gold(DECIMAL 金币，读表 ×10000 转铜，1金=10000铜，SharedDefines MoneyConstants COPPER=1/SILVER=100/GOLD=10000)。Sell() 改为：规则非空时走新函数 SellByRules；规则命中池由 InitializeBins 逐规则 MatchRule 填充到 rule.Items(不再用 14 品质 bin)。定价字段是每件单价，总价=单价×堆叠。MatchRule 改单规则匹配。
- 纠正"上架比例"语义：用户澄清=每类物品在AH的存量上限(非补货权重)。原 weight 版缺陷：只控制抽中概率、无每类封顶，某类命中物品多会被反复上架直到填满全局 maxitems、挤掉其它类。改为 max_count(该类挂单堆数上限，0=不限)，规则表 weight→max_count；SellByRules 选品改为"只收集未达上限(Count<MaxCount)的规则随机选"，全满即停；SellerRule 加 Count 字段，由 AuctionHouseBotAuctionHouseScript 的 OnAuctionAdd/OnAuctionRemove 钩子 MatchRule 命中则 ±1 维护(与 IncItemCounts/DecItemCounts 同位置)。存量计数口径与品质计数一致(默认全部挂单，ConsiderOnlyBotAuctions 时仅 bot)。三阵营 AHBConfig 各自独立 Count，max_count 分别生效。
- 用户重新完整提需求，判定"改造 mod-ah-bot 不如重做"，产出 outputs/mod-ah-seller拍卖行卖家池模块设计方案.md。结论：需求(类型池统一价/entry池单独价/每池max_count+补货间隔+补货量+时长默认12h+独立上下浮比例)与 mod-ah-bot 的 14品质bin+14档占比+全局过滤+买家+市场价模型完全不匹配，改造=拆大半重建，重做更简单干净。设计：新模块 mod-ah-seller，两张表 ah_seller_pool(池子, pool_type 1类型/2entry, auctionhouse 2/6/7/0全部, class/subclass/等级/品质筛选, buyout/bid_price_gold, price_up/down/step_pct 浮动, max_count/restock_interval/restock_count/duration_hours) + ah_seller_pool_items(entry池物品, 每entry单独价)。金额优先级:物品单独价>池子价>SellPrice(全0跳过)。浮动:heat 内存表(成交+1/过期-1, step%步进, clamp±up/down)，OnAuctionSuccessful/Expire 钩子维护。补货:OnBeforeAuctionHouseMgrUpdate 时间戳比较(同 Buy bidinterval)。复用点:mod-ah-bot Sell()795-923上架核心段、Update()1029-1036虚拟玩家、getStackCount、MailScript拦截、成交/过期钩子。
- 【落地】新建并实现独立模块 modules/mod-ah-seller/（不基于 mod-ah-bot，用户之后会删 mod-ah-bot）。最终决策：不分阵营(固定挂中立 AH AuctionHouseId::Neutral=7)、重启不保留浮动(heat 内存)、不设价=用 SellPrice(为0忽略)、浮动0=不记录不调价、每池 stack_count 堆叠(不支持堆叠的物品自动1件)。文件：src/AhSeller.h(结构+全局状态)、src/AhSeller.cpp(加载池子/补货/定价/浮动/上架)、src/AhSellerScripts.cpp(WorldScript+AuctionHouseScript+MailScript+Addmod_ah_sellerScripts 入口)、conf/mod_ah_seller.conf.dist、data/sql/db-world/mod_ah_seller.sql。模块发现机制：GetModuleSourceList 用 file(GLOB) 扫 modules/ 子目录，无需 CMakeLists.txt；入口函数名 Addmod_<目录名>Scripts()(- 转 _)；MODULES=static 自动静态编译。
- 【回退】按用户要求撤销 mod-ah-seller 第二轮复核修改，C++、conf、SQL 和设计文档均恢复到首次实现版本，并删除第二轮复核报告。第二轮曾发现的堆叠、重叠池、校验与溢出风险也随回退重新存在；此前钩子时序调查结论仅保留为历史分析，不再代表当前实现已修正。
- 【80级卖家池数据】按当前 acore_world.item_template 生成 outputs/80级卖家池SQL/ 下8个可重复执行的 Entry 池SQL（801稀有坐骑、802绿色宝石、803紫色宝石、804蓝色附魔卷轴、805紫色附魔卷轴默认禁用、806蓝色腿部附魔、807紫色腿部附魔、808装等>=213绑定装备）及 outputs/80级卖家池配置清单.md。验证命中：绿色19、紫色宝石35、蓝色卷轴34、蓝色腿部4、紫色腿部9、紫色卷轴0、绑定装备4773；固定稀有坐骑23项。绿色宝石按颜色名 Forest Emerald/Eye of Zul 且兼容 Quality 2/3；紫色宝石按 Dreadstone/Ametrine 并排除 RequiredSkill>0 的珠宝专业限制。SQL格式检查通过。
- 【坐骑注释】按用户要求将 outputs/80级卖家池SQL/01_稀有坐骑池.sql 中23个坐骑 Entry 后的英文注释全部改为中文，SQL逻辑与配置清单未变。
- 【类型池筛选】为 mod-ah-seller 的 pool_type=1 增加 RequiredLevel 范围筛选：新增 min_required_level/max_required_level 字段，0 表示不限；AhSeller.cpp 查询、结构体和 MatchTypePool 已同步，SQL 示例及设计文档已更新。使用现有完整 MSVC 编译参数对 AhSeller.cpp/AhSellerScripts.cpp 单独编译通过，git diff --check 通过。
- 【类型池 BagFamily】为 pool_type=1 增加 `bag_family` 位掩码筛选：0 表示不限，非0时使用 `(proto.BagFamily & pool.BagFamily) != 0` 命中任意配置位；同步结构体、数据库列、读取索引、SQL示例和设计文档。两个模块源文件使用完整 MSVC 参数编译通过，git diff --check 通过。
- 【编译验证】新增模块后须重新 cmake configure 才被发现。amd64 MSBuild 又崩溃(Access violation，即使 dangerouslyDisableSandbox 也崩，之前 08-13 记录"已修复"又复发)，VS 生成器 build/ 不可用；改走 Ninja build-ninja(PowerShell 注入 MSVC/SDK 环境 + cmake -G Ninja 传 cl.exe/ninja/MySQL 路径，命令见 2026-08-13 日志)。Ninja configure 成功(mod-ah-seller 入列)，全量编译 1323 单元：我的 AhSeller.cpp/AhSellerScripts.cpp 编译通过(零错误)、modules.lib 链接成功，证明代码正确；但最终 worldserver.exe 链接前 worldserver.rc.res 编译失败(cmcldeps.exe + rc.exe 报 Permission denied/Access denied 删除 depfile/打开 worldserver.i)，属环境文件锁问题、与代码无关。用户可在自己环境(若 MSBuild 修复)或用 VS 重试完成最后链接。
