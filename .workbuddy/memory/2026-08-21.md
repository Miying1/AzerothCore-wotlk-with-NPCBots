- 修复"午夜冲锋秒人"：冲锋实为骑乘版阿图门(16152)的 Charge 29847，DBC 显示是武器伤害型（Effect2=58 武器伤害+74，非纯控制），随 DmgMod=77 缩放后伤害≈177.07×77+74≈13,710，选 8-25 码远程目标，对布甲暴击≈27,000 秒杀。
- 修复：新增 spell_karazhan_attumen_charge 脚本（SpellScript OnHit SetHitDamage 重写为固定 urand(8000,9000)），注册到 AddSC_karazhan_spell_scaling，SQL 单独绑定 29847→spell_karazhan_attumen_charge（独立脚本名，不混入 spell_karazhan_direct_scale）。
- 关键教训：判断"控制类技能"时不能只看技能名/文档标注，要查 DBC Effect 字段——冲锋 29847 是 Effect96(位移)+Effect58(武器伤害)+Effect64(触发34719近战急速)，含武器伤害成分，之前 §5.1 误标为[控]遗漏。武器型技能随 DamageModifier 缩放，升级 DmgMod 会放大这类"打远程"技能的伤害。
- 撤销 29711「击倒」的缩放：29711 是午夜(Midnight)的技能，DBC 显示 Effect1=2 直伤(固定1500, base1499+die1) + Effect2=6 光环 ApplyAura=12(SPELL_AURA_MOD_STUN 昏迷/击倒控制)，无触发、无武器伤害成分。本质是击倒控制技能+象征性1500固定小伤，之前误按"控制+小伤"缩放3.0×(1500→4500)，违背"控制技能不缩放"原则。
- 已从三处移除：C++ 倍率表、技能伤害缩放SQL.sql、设计文档(代码块+SQL块+§6.1表格)，并在 §8 不缩放清单新增"控制|午夜·击倒|29711|昏迷控制(附带1500固定小伤)"条目。缩放列表 75→74 个法术，C++与SQL校验一致。
- 更正：撤销 29711「击倒」的做法已回退。用户指出项目里并无"控制技能不缩放"原则——"不缩放"的真实依据是技能"无伤害"（缩放无意义）、武器型走 DmgMod、百分比/机制型锁血等，而非"控制"标签。29711 击倒有 Effect1=2 直伤(固定1500)，属"有伤害"，应纳入缩放。
- 已恢复 29711 为 3.0×（1500→4500），回到 C++ 倍率表、技能伤害缩放SQL.sql、设计文档(代码块+SQL块+§6.1表格)，并从 §8 不缩放清单移除"午夜·击倒"条目。缩放列表 74→75 个法术，C++与SQL一致。
- 经验教训：给技能定"不缩放"时，必须依据 DBC Effect 字段判断"是否有伤害"（Effect2直伤/Effect6+周期伤害/Effect58武器/Effect64触发），不能按"控制/减益"这种语义标签一刀切。类似地，之前冲锋 29847 也是因漏看 Effect58 武器伤害成分才误判为[控]。
- 删除 spell_karazhan_attumen_charge 冲锋重写脚本：移除 C++ 类定义+RegisterSpellScript 注册+#include "Random.h"（urand 仅此脚本使用）、SQL 中 29847 的 DELETE/INSERT 绑定、设计文档 §8 该条目改为"武器型，走 DmgMod"。
- 后果：冲锋 29847 恢复原生武器伤害型，随 DmgMod(77) 缩放 ≈13,710，会再次对布甲秒杀（此前重写为固定 8000-9000 正是为防秒人）。C++ 倍率表仍 75 个法术，与 SQL 一致。
- 发现贞洁圣女"圣洁之地"(Holy Ground)=29523 在 Spell.dbc 里缺失（29520-29528 区间跳号 29523），脚本 boss_maiden_of_virtue.cpp:35 的 DoCastAOE(29523,true) 静默失败，技能无效、无伤害。全 src 仅此一处引用，SpellInfoCorrections 无补正。属"断链引用"（同 29767/29857 类）。
- 复核盲区教训：C++ BossAI 脚本里硬编码的 DoCastAOE/DoCast spell ID 不进 smart_scripts 表，之前"从 smart_scripts 拉施法记录"的复核方式覆盖不到；须额外核对 C++ Boss 脚本的 SPELL_* 枚举。
- 整理卡拉赞各 Boss 的 lootid（查 acore_world.creature_template.lootid + creature_loot_template 校验），并更新设计文档《卡拉赞80级10人升级设计文档.md》三处表格（§执行摘要 Boss 目标参数表、§4.3 可选 Boss、§4.4 歌剧院）各加 LootID 列。关键发现：阿图门掉落挂骑乘版 16152（非骑乘 15550 lootid=0）、午夜 16151 无独立掉落、歌剧院 Oz 四单位(17535/17546/17543/17547) lootid=0 掉落集中在老巫婆 18168；其余 Boss LootID=自身 Entry。
- 生成卡拉赞 Boss 新装备掉落绑定 SQL（outputs/卡拉赞/卡拉赞Boss装备掉落绑定SQL（依赖装备SQL先执行）.sql）。机制确认：reference_loot_template 装备池编号 230=914000/240=924000/250=934000/260=944000/270=954000（generate_wotlk*.py 生成，池内装备 GroupId=1+Chance=0 等概率选1件）。掉落：250=阿图门16152/莫罗斯15687/贞洁圣女16457 各2件+剧院4单位(老巫婆18168/大灰狼17521/罗密欧17533/朱丽叶17534)各1件；260=馆长15691/泰雷斯蒂安15688/埃兰之影16524/虚空幽龙15689 各2件；270=玛克扎尔王子15690/夜之魇17225/泰恩里斯28194 各2件。关键坑：这些 Boss 原掉落已占用 GroupId 1~3，新槽须用 GroupId 10 避免互斥。装备 item_template 与 reference 池均尚未导入数据库。
- 更正掉落槽方案：不用"掉N件=N个GroupId槽"，改为"单槽(GroupId=10)+MinCount=MaxCount=N 控制件数"。源码 LootMgr.cpp 约束 reference 记录 MinCount 必须==MaxCount（否则 ReportInvalidCount 报错），MaxCount 决定 reference 池 Process 次数（每次从池内等概率选1件）。最终 14 条记录：10 主线 Boss MinCount=MaxCount=2，剧院 4 单位 MinCount=MaxCount=1。
- 卡拉赞象棋事件：给麦迪文的回响(16816)的 gossip 增加"开门!没空陪你下棋,你个老6"跳过选项（boss_chess_event.cpp）。新增 enum MEDIVH_GOSSIP_SKIP_CHESS(=4)，sGossipHello 无条件追加该菜单项，sGossipSelect 新增 case：清理棋盘(REINIT_PIECES+_deadCount.fill) + 移除 GAME_IN_SESSION + SetData(DATA_CHESS_EVENT, DONE) 打开通往王子的门(184277)。需重编译 worldserver 生效。
- 清除卡拉赞 Boss 原版 Reference 掉落：查库确认 14 个 Boss 中 10 个挂原版 T4 装备池(reference 34016~34023/34031/4113)，已 DELETE creature_loot_template 里这 10 条 reference 条目（Entry 16152/15687/16457/15688/16524/15689/17225/18168/17521/28194，验证后这 14 Boss 的 Reference>0 计数=0）。王子15690/馆长15691/罗密欧17533/朱丽叶17534 无 reference（用直接 Item 掉落），未动。生成 outputs/卡拉赞/卡拉赞Boss原版Reference掉落清除SQL.sql（含回滚 INSERT 注释块）。注：这些 reference 条目 Item 字段与 Reference 字段同值(如34017/34017)，引擎判断 reference 只看 Reference 字段(IsValid 的 `if(!reference)` 分支)；清除后 34016 等池成孤儿，启动会打印 "isn't referenced and thus useless" 警告，不影响功能。
- 将清除原版 Reference 掉落的 DELETE 合并进 outputs/卡拉赞/卡拉赞Boss装备掉落绑定SQL（依赖装备SQL先执行）.sql（按用户要求未执行数据库）。合并后执行顺序：SET 变量 → 清除旧 250/260/270 绑定 → 清除原版 34016~34023/34031/4113(10条) → INSERT 新装备 → 审核查询；头注释"不改原掉落"改为"清除原版掉落"。
- 更新 230/240/250/260 装等 PVE 装备名称（共 2324 件），风格从固定"秘星·星纱X洞察/迅捷/破阵/无瑕"改为 270 装等的魔兽自由命名（地域/称号/意象/动作/装备称谓混合、非固定句式）。生成器 var/rename_equip_230_260.py（固定种子 20260821），词库按槽位/武器subclass/定位风味分类；只改 item_template.sql 的 name 字段和清单.csv 的"名称"列，其余字段 byte 级不变。校验通过：全局唯一、与 270/280 不冲突、无数字/禁用词/多次"·"。备份在 outputs/装备生成/backup_名称更新前/，对照表 230-260装备名称更新对照表.csv。教训：Python re 里 `^(@...)` 的 `(` 是分组符非字面量，要匹配 SQL 行的字面 `(` 须用 `\(`，否则静默不匹配导致替换失败。
