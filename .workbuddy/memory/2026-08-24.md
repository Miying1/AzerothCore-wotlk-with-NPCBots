# 2026-08-24

- 复核并更新卡拉赞 80 级 10 人强度配置：在 `outputs/卡拉赞/卡拉赞80级10人生物强度调整SQL.sql` 补齐 7 个遗漏的 Boss 战斗召唤模板为 82 级，包括馆长 4 种星界光芒（17096、19781–19783）、玛克扎尔之斧（17650）、血红幽魂（28232）和 12 分钟超时召唤的埃兰之影（18254）；明确机制载体保持原等级。
- 根据 `Spell_dbc.csv`、核心 `SpellEffectInfo::CalcValue` 和 `karazhan_spell_scaling.cpp` 实际倍率，重新计算并更新全部 76 个法术条目的伤害备注。计算纳入 DBC BasePoints/DieSides、float 乘法后 int32 截断，以及 29978、29956、30282 等法术的 `SPELL_ATTR0_SCALES_WITH_CREATURE_LEVEL` 额外等级缩放。
- 再次逐字段确认 DBC：增强表中仅 29978、29956、30282、29711 带 `Attributes & 0x00080000`；其中只有前三个因 `SpellLevel=70` 满足核心等级缩放条件，29711 的 `SpellLevel=0` 不会进入等级缩放。29978 同时由 83 级埃兰和 82 级超时影子施放，脚本备注已分别标出最终 43771/42772。
- 核对 29956/29978/30282 释放机制：29955 的 DBC Effect1 为每 1000ms 触发 29956，父法术 CastingTimeIndex=4（1000ms）、DurationIndex=28（5秒），所以一次奥术飞弹通道通常为 5 发；29978 不在埃兰超级技能随机列表中，而是在喝水结束或喝水被打断后反击施放，18254 SmartAI 为首次 1000ms、之后每 5000ms；30282 为瞬发、无 RecoveryTime/CategoryRecoveryTime，由夜之魇地面/空中调度器分别在约25s/14s后每3s检查，脚本仅保留40码外玩家目标。确认当前 `karazhan_spell_scaling.cpp` 中 30383 倍率仍为 4.0x，未擅自修改。
- 完成卡拉赞全部脚本的随机点名核对：严格真实玩家过滤的核心案例是埃兰 30004 Flame Wreath 的 SpellScript（`IsPlayer()`，最多2人）；其余大多数 `playerOnly=true` 调用在 NPCBots 分支允许 NPCBot，`SelectPlayerTarget` 仅优先真实玩家且无真实玩家时回退。纳入清单的 Boss 包括午夜/阿图门、馆长、莫罗斯、圣女、邪蹄、埃兰、歌剧院三剧目、虚空幽龙、夜之魇、玛克扎尔、Tenris 和侍从区 Hyakiss，并单独排除区域技能、随机位置技能、当前目标技能和棋盘事件目标。
