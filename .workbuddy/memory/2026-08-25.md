# 2026-08-25
- 已完成 outputs/装备生成 六组装备头部插槽批量修改：InventoryType=1 统一清除原槽，设置混沌槽编码1，并固定种子随机增加1-2个红黄蓝槽；同步 SQL、装备清单和设计说明，最终校验通过。
- 已完成 mod-zone-difficulty 地下城挑战安全修复：修正每日地图/实例ID/词缀边界，挑战生物长期引用改用 ObjectGuid，堵住战斗中关闭与击杀Boss后开启，配置热关闭真正停用；Encounter 使用 updated 和唯一ID去重，并持久化剩余时间、尾王及已完成集合；光环清理改为模块白名单，奖励限定有效遭遇参与者，开启/关闭按返回值反馈。新增 characters 待执行迁移 rev_20260825_01.sql。目标三个 C++ 编译单元已单独编译通过，完整 worldserver 构建仍被既有缺失 mod-ah-bot/src/AuctionHouseBot.cpp 阻塞。
- 完成 mod-ale + Rochet2/AIO 实现 NPCBot 仿玩家装备 UI 的可行性研究：当前 ALE 已具备 AIO 所需事件30、SendAddonMessage、定时器和主状态API，但没有 NPCBot 专用 Lua 方法；推荐在 ALE CreatureMethods 增加窄 C++ 桥接，复用 bot_ai 现有 18 槽装备、所有权、_canEquip/_equip/_unequip 规则，AIO 仅负责消息与客户端 FrameXML 展示。
- 继续核对 NPCBot 头像来源：项目没有客户端 Lua/XML/AIO 文件，NPCBot 通过服务器原生 Group/SMSG_GROUP_LIST 与 SMSG_PARTY_MEMBER_STATS 进入玩家队伍框架；Group::AddMember(Creature*) 将 Bot GUID、名称等写入成员槽，且 BotMgr 提供 BuildBotPartyMemberStatsPacket。用户实测队伍框架右键 NPCBot 能显示并执行“移出队伍”，说明该右键路径实际使用了队伍菜单上下文（通常是 PARTY 的 UNINVITE，团队中可能是 RAID_PLAYER/RAID 的 RAID_REMOVE），不能再按普通生物 TARGET 菜单处理。后续“查看”应插入 PARTY、RAID_PLAYER、RAID并过滤普通玩家；装备槽 Tooltip 可直接使用 GameTooltip:SetHyperlink(snapshot.itemLink)，ALE Item:GetItemLink 已包含物品ID、永久附魔、三宝石、额外附魔、随机属性/后缀和等级，效果可基本与背包Tooltip一致。
