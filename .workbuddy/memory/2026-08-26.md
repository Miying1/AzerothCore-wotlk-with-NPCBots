- 阅读 E:\workbuddy\AIO\README.md，确认 AIO 官方安装约定为 AIO_Server 放服务端 lua_scripts、AIO_Client 放 WoW Interface/AddOns。由于本项目 ALE 会递归执行 lua_scripts 下所有脚本，未直接放入 lua_scripts/AIO_Server，而是用仓库 AIO_Server 源码更新到项目 lua_modules/AIO，由 00_AIOBootstrap.ext 加载；保留 bit.lua 作为当前 Lua 5.2 兼容层。使用仓库 AIO_Client 更新 outputs/NPCBot/AIO_Client，服务端/客户端 AIO.lua SHA-256 均与仓库对应文件一致，git diff --check 通过。
2. 根据用户要求进一步将 NPCBotEquipment 界面仿制为玩家 PaperDoll 风格：主窗口改用 UIPanelDialogTemplate，增加玩家式深色对话框边框、左右内嵌面板、中间角色预览框和底部角色选项卡；装备槽使用对应 UI-PaperDoll-Slot-* 空槽纹理、Quickslot 边框、高亮与按下态，18 个内部槽保持唯一映射并按玩家装备面板视觉顺序分布；服务端仍不放客户端 UI 脚本。git diff --check 与结构断言均通过，尚未进行 WoW 3.3.5 实机像素和资源兼容验证。
3. 根据用户反馈简化 NPCBotEquipment 通信：名称、职业和模型不再由 BotEquipmentSnapshot/ALE 下发，已移除 botName、botClass、botDisplayId 及对应 Lua 字段；独立客户端插件改用 UnitName(unit)、UnitClass(unit) 和 PlayerModel:SetUnit(unit) 读取当前可见生物信息，标题仍为“名称[职业]”并按 RAID_CLASS_COLORS 着色。UI 继续复用 WoW 3.3.5 内置 DialogFrame、PaperDoll 空槽、Quickslot、Character Tab、Tooltip Divider 等材质，并增加标题分隔装饰；静态检查、18 槽映射和 git diff --check 通过。数字参数调试入口或目标离开客户端可见范围时只能回退为 NPCBot 标题和空模型。
4. NPCBot 装备界面快照缓存已调整为客户端 SavedVariables 持久化缓存：`NPCBotEquipmentCacheDB` 按 botEntry:guidLow 保存最多 32 条，TTL 为 3 天，跨客户端重启保留并自动清理过期项。装备/卸下成功改用增量快照：主手或副手变化时服务端仅构建并返回主副手两槽，其他装备仅构建并返回对应槽位；客户端合并槽位和新 revision 后回写持久缓存。失败与状态过期仍返回完整快照以恢复一致性。
5. NPCBot 装备协议进一步精简：装备槽保留 `slot/occupied/itemGuid/entry/link/quality/itemLevel`，候选仅下发 `itemGuid/entry/link/quality/itemLevel`；不再下发 icon、gearScore、count、durability、maxDurability、bag、bagSlot。客户端以 `GetItemIcon(entry)` 获取图标，并在读取旧 SavedVariables 时自动压缩迁移；候选 gearScore 与背包位置仍仅在服务端内部用于排序。
6. NPCBotEquipment“属性”页已实现实时数据链路：服务层按当前 spec/role 将 Bot 归类为物理近战、物理远程、治疗或法系远程；ALE/AIO 新增 `GetNPCBotAttributes`/`RequestAttributes`；客户端仿玩家属性面板显示顶部天赋、左侧固定防御属性、右侧按天赋显示主要属性。装备成功后会清除本次属性数据，重新进入属性页时刷新。静态结构断言与 `git diff --check` 通过；CMake 构建命令返回 1 且未输出诊断，直接 MSBuild 被安全策略拦截，尚需完整编译与 WoW 3.3.5 实机验证。
7. 按用户要求从 NPCBot 属性页协议中彻底移除韧性：删除服务层 `resilience` 字段及计算、ALE 下发字段和客户端防御属性行；无残留引用，`git diff --check` 通过。
8. NPCBot 装备快照与候选信息重新加入单件 `gearScore`，快照新增所有 18 个装备槽 GS 汇总的 `totalGearScore`；完整快照和装备增量快照均返回最新总 GS。客户端装备槽底部居中显示物品等级，模型区武器栏上方居中显示 `GS:总数`；SavedVariables 缓存协议升级为版本 2，自动丢弃不含 GS 的旧缓存。格式检查通过，尚需完整编译和实机布局验证。
9. NPCBotEquipment 权限模式改为主人/访客两级且不考虑共享者：其他玩家可读取当前视野内 NPCBot 的装备和属性，但 `canManage=false`，客户端仅显示居中的“装备/属性”两页且禁用右键候选；真正主人 `GetBotOwnerGuid/GetBotOwner` 双重匹配时 `canManage=true`，显示“装备/属性/管理”并可装备、卸下。服务端所有候选和变更入口继续通过 `ValidateOperation` 强制校验真正主人，不能靠伪造客户端字段绕过。缓存版本升至 3，缓存键加入查看者角色 GUID，避免跨角色复用管理权限。
10. NPCBotEquipment 客户端已按职责拆为 `NPCBotEquipmentClient.lua`（共享框架、装备快照/缓存、候选及装备操作）、`NPCBotEquipmentAttributes.lua`（属性面板、请求、响应和超时）与 `NPCBotEquipmentManagement.lua`（主人专属管理页），TOC 按此前后顺序加载。访客界面已彻底移除“只读查看”提示并静默屏蔽右键候选；缓存命中后仍后台请求最新快照以校验 revision 和当前权限。复核同时修复候选响应应用完整快照时误关闭候选面板、属性异步响应切页后重新显示属性层的问题；四文件结构断言及空白检查通过。
11. NPCBot 战斗走位改为单 Bot 运行时覆盖：保留原有对话菜单入口和布局，点击原“战斗走位”选项仅切换当前 Bot 的启用/禁用状态；新增覆盖字段不写数据库，重置或重新初始化时恢复默认继承主人设置。
11. 为 NPCBot 增加单Bot战斗走位三态运行时覆盖：在 BotAI 中加入继承主人设置/单Bot启用/单Bot禁用状态，接入战斗站位、跟随距离约束和战斗更新路径；对话菜单新增“战斗走位...”子菜单，不写数据库，ResetBotAI 后恢复继承。新增世界数据库更新中的 70701-70707 菜单文本及 zhCN 本地化。未执行完整构建和实机验证。
12. NPCBotEquipment“管理”页已从占位页实现为主人专属配置 UI：按支持项横向显示主坦克/副坦克/伤害输出/治疗/远程战斗复选框，主副坦克在客户端和服务端双重联动；治疗职业显示 1-100 整数生命阈值；进战延迟支持 0-10 秒并同时写入 BotMgr 的 DPS/Heal 延迟；攻击角度为普通/避开正面 AOE 单选；战斗走位写当前 Bot 的运行时覆盖。新增 `BotManagementSnapshot`、ALE Get/Update 接口及 AIO 读取/更新 Handler，服务端强制真正主人权限并返回规范化状态。静态断言与 `git diff --check` 通过；完整编译仍被 amd64 MSBuild Access violation、x86 MSBuild 安全拦截和 Ninja `bad $-escape` 阻塞。
