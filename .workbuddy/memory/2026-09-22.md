# 2026-09-22 工作日志

## 奥杜尔 Boss 点名技能改造：只点真实玩家（排除 NPCBot）

任务：将奥杜尔「点名目标」技能的目标选择改为只点真实玩家（排除 NPCBot），例外三处：
- 米米尔隆等离子冲击（Plasma Blast，实际施放给主坦，非随机点名，不改）
- 科拉隆石握（Stone Grip，改动范围另行处理）
- 霍迪尔冻结（Freeze，不改）

复用项目已内置的 `UnitAI::SelectPlayerTarget` / `SelectPlayerTargetList`（UnitAI.h:283/287，
内部 `target->IsPlayer() && !target->IsNPCBot()`，含兜底回退）以及 `IsNPCBot()`（Object.h:202 基类虚函数，WorldObject 可直接调用）。

改动清单（7 处 IsNPCBot + 3 处 SelectPlayerTarget）：
1. `boss_general_vezax.cpp` L214 影爆 Shadow Crash：加 `!temp->IsNPCBot()`
2. `boss_general_vezax.cpp` L250 无面者印记：加 `!tmp->IsNPCBot()`
3. `boss_mimiron.cpp` L1101 凝固汽油弹 Napalm Shell：加 `!plr->IsNPCBot()`
4. `boss_ignis.cpp` L348 抓取 Grab/熔渣罐：加 `temp->IsNPCBot()`
5. `boss_xt002.cpp` L725 灼热之光/引力炸弹 FilterTargets：RandomResize 前 remove NPCBot
6. `boss_kologarn.cpp` L640 凝视之眼：NonTankTargetSelector 组合 lambda 追加 `!IsNPCBot()`
7. `boss_freya.cpp` L1212 不稳定阳光 FilterTargets：RandomResize 前 remove NPCBot
8. `boss_freya.cpp` L554/887/1106：`SelectTarget` → `SelectPlayerTarget`（阳光/荆棘群/闪电鞭）

未改造（需后续确认）：芙蕾雅铁根 SPELL_IRON_ROOTS(62275)/SPELL_IRON_ROOTS_FREYA(62862) 无 SpellScript 过滤，
走 DBC ImplTargetA=22 随机点 1 人，为控制类（根须缠绕定身），非严格"离群"技能。

关键认知：NPCBots 是 Player 类型但 `IsNPCBot()` 返回 true；`NonTankTargetSelector` 的 playerOnly
只判断 `IsPlayer()`，无法区分 NPCBot，需组合 lambda 附加过滤。

## 科拉隆凝视之眼 NPCBot 危险区域配置

在 `outputs/NPCBot/机制处理/危险区域识别.sql` 追加凝视之眼配置：
- 左眼 33632 / 右眼 33802，map 603（奥杜尔）
- radius=7.0、damage_spell_id=0（射线 63676/63702 为 ImplTargetA=38 定向光束，半径 0 读不出）、
  safety_distance=1.5、deactivation_delay_ms=1500
- 机制：眼睛 MoveChase 追人被点名玩家，危险圈随眼睛实时位置移动（CollectCreatureHazards
  每帧用 creature->GetPosition() 取实时位置，天然支持移动生物）。

## 奥杜尔其它 Boss 危险区域候选排查（供用户决策，未写入 SQL）

已配置（危险区域识别.sql）：米米尔隆火焰扩散 34121、烈焰巨兽灼热地面 33123、科拉隆凝视之眼 33632/33802。

候选「可加入」：
- 芙蕾雅 自然炸弹 NPC_NATURE_BOMB=34129，伤害 SPELL_NATURE_BOMB_DAMAGE=64587（点玩家位置召唤，11s 后爆炸，固定位置炸弹，BOT 应远离）
- 奥尔加隆 黑洞 NPC_BLACK_HOLE=32953，伤害 SPELL_BLACK_HOLE_DAMAGE=62169（持续地板陷阱）
- 奥尔加隆 虚空带 NPC_ALGALON_VOID_ZONE_VISUAL_STALKER=34100，伤害 SPELL_SUMMON_VOID_ZONE_VISUAL=64470（环境伤害，固定地板）
- 米米尔隆 感应地雷 NPC_PROXIMITY_MINE=34362（踩踏触发爆炸）

排除（不适用）：
- 萨隆邪雾 33488（玩家主动吸蓝资源，BOT 不该躲）
- 崩塌星 32955 / 活体星座 33052（玩家要打的怪，非纯地板）
- 灼热之光/引力炸弹（单体点名，已改只点真实玩家）
- 霍迪尔冰锥/冻结（单体点名或全体机制）

## 危险区域识别.sql 最终写入（本轮完成）

在 `outputs/NPCBot/机制处理/危险区域识别.sql` 追加四条（map 603 奥杜尔）：
- 凝视之眼 33632/33802，radius=10（用户确认为 10，非 7）
- 自然炸弹 34129，radius=10 + damage_spell_id=64587
- 黑洞 32953，radius=8 + damage_spell_id=62169（周期伤害无半径，靠固定 radius）
- 虚空带 34100，radius=10 + damage_spell_id=64470
- 感应地雷 34362，radius=8 + damage_spell_id=66351
均带 DELETE 幂等处理。


