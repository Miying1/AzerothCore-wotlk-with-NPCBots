# 项目长期约定

- 本地MySql服务用户:root 密码:wow@80 ,对应该项目的数据库:acore_auth\acore_characters\acore_world
- `outputs/` 下交付给用户的报告、设计文档和资源清单默认使用清晰的中文文件名；文档内部交叉引用必须同步使用中文路径。源码、脚本、SQL迁移等受工程命名规范约束的文件不因此改为中文。
- 项目新增或修改的源码注释默认使用简体中文；代码标识符、API名、Spell ID、数据库字段等工程术语保持原名，避免为中文化破坏可检索性。
- 五人英雄裂隙源码统一组织在 `src/server/scripts/HeroicDungeonRift/`：根目录放共享代码、配置、传送和管理代码；普通源码文件及 `AddSC_` 注册函数使用 `rift_` 前缀，Boss脚本使用 `boss_rift_` 前缀，模块总入口为 `AddHeroicDungeonRiftScripts()`；每个目标副本建立独立子目录，副本适配代码和该副本所有Boss脚本均放入对应子目录，不散落到根目录或原版副本脚本目录。
- 五人英雄裂隙最终 Entry 分配（不可与其它区间重叠）：入口NPC 100000；全部69个Boss的T1/T2/T3连续为100001–100207（公式 `100001 + (boss_id-1)*3 + (tier-1)`），其中60级boss_id 1–47为100001–100141、70级boss_id 48–69为100142–100207；全部专用同伴/召唤物为102000–102058，其中60级102000–102033、70级102034–102058；出口门100500–100502。主Boss 83级、召唤物82级；同一Boss三档共用同一 `boss_rift_*` 脚本，双Boss同伴使用 `npc_rift_*` 模板协作。
- 构建环境约束：amd64 版 MSBuild.exe 曾启动即崩溃（0xC0000005 访问冲突，仅 amd64 受影响、x86 版正常），导致 VS 解决方案生成器（`build/`、.slnx）一度不可用、只能走 Ninja + VS2026 MSVC（`build-ninja/` 目录）；2026-08-13 用户反馈该 MSBuild 崩溃问题已修复，VS 生成器恢复可用（项目根 `CMakePresets.json` 的 `win-vs2026-x64` preset → `build/AzerothCore.slnx`）。Ninja 方案仍保留为备选。工具链：MSVC 14.51.36231（VS2026 Community 18.8）、CMake 4.3.1（VS 自带）+ 4.4.2（独立安装，`C:\Program Files\CMake\bin`，含 cmake-gui.exe）、MySQL 8.0.46（`D:/Program Files (x86)/mysql`）、Boost 1.91.0（`C:/local/boost_1_91_0`）、Windows SDK 10.0.22621.0。重新配置方法（已验证）：`Launch-VsDevShell.ps1` 在非交互模式会卡在 `Read-Host`（检测到多个 VS 安装要求选择）而失败，且 PowerShell 工具禁用 cmd.exe（不能用 `cmd /c vcvars64.bat && ...`），故须在 PowerShell 里手动注入 MSVC/SDK 环境变量（`$env:PATH` 追加 `MSVC\bin\Hostx64\x64` 与 `SDK\bin\<ver>\x64`；`$env:INCLUDE` 设为 MSVC include + SDK 的 ucrt/shared/um/winrt；`$env:LIB` 设为 MSVC lib\x64 + SDK ucrt/um\x64），再调 cmake 4.4.2 显式传完整 `cl.exe` 路径与 Ninja 路径（`-DCMAKE_C_COMPILER/CMAKE_CXX_COMPILER/CMAKE_MAKE_PROGRAM`），并补 `-DMYSQL_INCLUDE_DIR=...\mysql\include -DMYSQL_LIBRARY=...\mysql\lib\libmysql.lib`（CMake 自动找不到 MySQL）。完整命令见 2026-08-13 日志。
- 物品学习技能 SQL：AzerothCore 特殊学习物品使用 `spellid_1=483`、`spelltrigger_1=0`，目标法术放在 `spellid_2`，并设 `spelltrigger_2=6`。本项目自定义物品 `指位技能` 使用 Entry 93003，更新文件位于 `data/sql/updates/pending_db_world/rev_20260815_01.sql`。
