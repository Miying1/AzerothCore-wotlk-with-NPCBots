-- 地下城挑战：补齐实例恢复字段，兼容已有 zone_difficulty_instance_saves 表
ALTER TABLE `zone_difficulty_instance_saves`
  ADD COLUMN IF NOT EXISTS `level` INT UNSIGNED NOT NULL DEFAULT 0 AFTER `InstanceID`,
  ADD COLUMN IF NOT EXISTS `enhance_damage` INT UNSIGNED NOT NULL DEFAULT 0 AFTER `level`,
  ADD COLUMN IF NOT EXISTS `enhance_hp` INT UNSIGNED NOT NULL DEFAULT 0 AFTER `enhance_damage`,
  ADD COLUMN IF NOT EXISTS `residue_time` INT UNSIGNED NOT NULL DEFAULT 0 AFTER `enhance_hp`,
  ADD COLUMN IF NOT EXISTS `kill_boss` INT UNSIGNED NOT NULL DEFAULT 0 AFTER `residue_time`,
  ADD COLUMN IF NOT EXISTS `spell_id1` INT UNSIGNED NOT NULL DEFAULT 0 AFTER `kill_boss`,
  ADD COLUMN IF NOT EXISTS `spell_id2` INT UNSIGNED NOT NULL DEFAULT 0 AFTER `spell_id1`,
  ADD COLUMN IF NOT EXISTS `spell_id3` INT UNSIGNED NOT NULL DEFAULT 0 AFTER `spell_id2`,
  ADD COLUMN IF NOT EXISTS `is_complete` TINYINT UNSIGNED NOT NULL DEFAULT 0 AFTER `spell_id3`,
  ADD COLUMN IF NOT EXISTS `last_boss_killed` TINYINT UNSIGNED NOT NULL DEFAULT 0 AFTER `is_complete`,
  ADD COLUMN IF NOT EXISTS `completed_encounters` TEXT NOT NULL AFTER `last_boss_killed`;
