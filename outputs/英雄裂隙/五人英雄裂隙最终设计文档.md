# 五人英雄裂隙最终设计文档

> 适用版本：AzerothCore WotLK 3.3.5a + NPCBots
>
> 本文是五人英雄裂隙的唯一最终设计文档。此前裂隙传送、简化配置、入口解耦、Boss模板和出口门的重复设计均以本文为准。

## 1. 最终设计目标

五人英雄裂隙是海加尔山开放世界中的旧世界副本挑战入口。玩家从海加尔山的裂隙、入口NPC或其他受支持的Gossip入口发起挑战，选择T1、T2或T3后，由服务器随机选择一个启用的旧世界副本Boss，创建该Boss对应的实例场景，并将队伍传送到Boss战斗区域。

每场裂隙只激活一个Boss，不要求玩家清理副本小怪，也不把入口永久绑定到某一张副本或某一个Boss。

核心原则：

- 最多5个战斗席位，允许少于5人进入；
- Boss不根据真实玩家数量、NPCBot数量或Free Bot数量动态缩放；
- 同一个Boss的T1/T2/T3生物Entry全部使用同一个Boss C++脚本；
- 该Boss的强度和技能差异全部在这一份脚本内通过T1/T2/T3控制；
- 实例运行状态只存服务器内存，不写运行态数据库；
- T1/T2/T3分别使用出口GameObject Entry 100500/100501/100502和模型7148/8196/8197，但共用同一个 `rift_exit_portal` 脚本与固定对话“离开...”；本场只记录发起入口对话者的位置，所有通过出口门离开的玩家都返回这一处共享位置；
- 不伪装LFG，不引入LFG随机本状态、投票踢人、逃亡者、补位Bot等额外耦合。

## 2. 入口与随机流程

### 2.1 入口解耦

入口对象不负责绑定某一副本或Boss，只负责让队长选择Tier并发起挑战。可支持：

- 海加尔山固定位置的裂隙GameObject；
- 选择Tier的入口NPC；
- 后续扩展的其他CreatureScript或GameObjectScript入口。

不同入口统一调用同一个启动服务，不在各自Gossip脚本中重复编写副本随机、Boss随机、实例创建、传送、返回点和出口门逻辑。

### 2.2 Gossip选项

入口提供以下固定选项：

```text
进入 T1
进入 T2
进入 T3
取消
```

客户端Action只表达Tier选择，不携带可信的Boss ID、地图ID、实例ID、坐标或返回坐标。服务器收到选择后，从启用配置中执行随机：

```text
玩家/队长选择Tier
    ↓
校验队伍和战斗席位
    ↓
从配置表1筛选 enabled=1 的Boss
    ↓
联结配置表2中相同 boss_id + tier 的行
    ↓
按 map_id 分组并等概率随机一个副本
    ↓
在该副本的合法候选中随机一个Boss
    ↓
创建或加入对应 map_id 的普通 InstanceMap
    ↓
在Boss生成点召唤该Tier的 entry_id
    ↓
将真实玩家传送到该Tier最终玩家传入点
```

候选Boss必须同时满足：

- 配置表1和表2均存在；
- `enabled=1`；
- 选择Tier对应的 `entry_id` 存在；
- `map_id` 可创建实例；
- Boss生成点通过地图坐标、高度和碰撞校验；
- 玩家传入点通过地图坐标、安全高度、可行走和距离校验。

如果候选无效，直接排除并重新随机，不能使用任意默认副本入口或默认Boss坐标兜底。

## 3. 队伍和NPCBot规则

### 3.1 战斗席位

- 队伍最多5个战斗席位；
- 允许1至5个真实玩家进入；
- 不按队伍人数改变Boss血量、伤害、技能数量或技能强度；
- NPCBot不作为裂隙Boss倍率计算依据。

### 3.2 玩家传送

只有真实玩家由裂隙模块主动传送。流程为：

1. 发起入口对话的玩家确认Tier；
2. 只记录该对话发起者在进入裂隙前的位置，作为本场全队共享返回点；不记录其他队员各自的位置；
3. 服务器创建新的普通副本实例并取得其 `InstanceSave`；
4. 在任何成员开始传送前，将全部真实玩家临时绑定到同一个 `map_id + instanceId`，避免远距离传送确认竞态导致成员分流；
5. 再将全部真实玩家传入该实例，并在配置的唯一玩家传入点周围分配安全偏移；
6. Boss在配置的Boss生成点召唤并进入战斗准备流程。

### 3.3 NPCBot进入

- 已经加入队伍并且跟随主人的玩家NPCBot，不由裂隙脚本直接传送；
- 主人完成远距离传送后，继续使用现有NPCBot跟随和 `BotMgr::OnTeleportFar` 机制自动进入；
- Free Bot不进入裂隙；
- 不为NPCBot额外分配独立的玩家传入偏移点；
- 不因为Bot是否及时进入而重新计算Boss强度。

## 4. 副本与Boss坐标

### 4.1 副本场景

旧世界副本通常没有可直接复用的原生英雄地图难度，因此使用对应普通副本的 `map_id` 作为场景外壳。T1/T2/T3不依赖核心的客户端地下城难度分支，而由裂隙配置、独立Boss Entry和Boss脚本控制。

### 4.2 Boss生成点

配置表2中的 `boss_spawn_x/y/z/o` 使用现有副本原版Boss的实际生成坐标。每场只召唤新的T1/T2/T3 Entry，原版静态Boss不参与本场战斗。若原版Boss没有静态 `creature` 刷新记录，则使用原版实例脚本或SmartAI中的召唤点；例如地狱火城墙的维斯路登遭遇由17307控制器在 `-1406.5,1746.5,81.2,5.46` 召唤17537维斯路登和17536纳杉。裂隙移除前置控制器条件，直接让裂隙主Boss可攻击，并在其35%生命阶段召唤裂隙纳杉同伴。

Boss生成点应在导入配置前完成：

- `map_id`一致性检查；
- `IsValidMapCoord`检查；
- 地图高度和碰撞检查；
- 与玩家传入点的距离和可达性检查。

### 4.3 玩家唯一传入点

玩家传入点不是代码常量，由用户或运营者为每个Boss提供并写入配置。该点应位于Boss房间内安全、可行走的位置，可以与Boss生成点不同。基础表允许四个传入坐标同时为NULL，用于表示尚未完成的运营配置；四个坐标不得部分为空。70级配置当前遵循“玩家传入点先留空”的交付边界：基础表和Tier表的四个传入坐标均保持NULL，并将基础配置 `enabled=0`，避免未完成配置进入随机池；已核对的候选传入点只保留在 `outputs/英雄裂隙boss传入点_整合.xlsx`，经过实际地图高度、碰撞、可达性和距离测试后才回填Tier表并启用对应Boss。运行时不使用任意默认入口或未配置坐标兜底。

同一场小队使用同一个配置传入点。真实玩家在该点周围按任意方向偏移1至2码：

```cpp
angle = random(0.0f, 2.0f * PI);
radius = random(1.0f, 2.0f);
x = entryX + cos(angle) * radius;
y = entryY + sin(angle) * radius;
groundZ = map->GetHeight(x, y, entryZ + 2.0f, true, 5.0f);
z = groundZ有效 ? groundZ : entryZ;
orientation = atan2(bossY - y, bossX - x);
```

每个候选落点都必须执行地图坐标、碰撞、高度、可行走和与Boss距离检查。找不到合法散点时，只能在同一传入点附近使用经过校验的备用偏移，不得把玩家发送到未配置的默认入口。

## 5. 两张静态配置表

数据库只保存Boss静态配置，不保存实例运行态。

### 5.1 配置表1：`heroic_dungeon_rift_boss`

一行代表一个可进入随机池的Boss基础配置。

| 字段 | 类型 | 说明 |
|---|---|---|
| `boss_id` | `INT UNSIGNED` | 自定义Boss ID，主键；用于C++ Boss逻辑分派 |
| `map_name` | `VARCHAR(64)` | 副本名称，供管理界面和日志显示 |
| `map_id` | `SMALLINT UNSIGNED` | AzerothCore副本地图ID |
| `dungeon_version` | `TINYINT UNSIGNED` | 地下城版本，只允许 `60` 或 `70`；已有经典配置使用60，燃烧的远征配置使用70 |
| `player_entry_x` | `FLOAT NULL` | 默认玩家传入点X；暂未配置时允许与其余三个坐标一起为NULL |
| `player_entry_y` | `FLOAT NULL` | 默认玩家传入点Y |
| `player_entry_z` | `FLOAT NULL` | 默认玩家传入点Z |
| `player_entry_o` | `FLOAT NULL` | 默认玩家传入朝向 |
| `enabled` | `TINYINT UNSIGNED` | 是否进入随机池；传入点未完成的BOSS必须为0 |
| `remark` | `VARCHAR(255)` | 坐标来源、Boss备注和维护说明 |

`boss_id`是自定义配置ID，不等同于核心的 `creature_template.entry`。

### 5.2 配置表2：`heroic_dungeon_rift_boss_tier`

一行代表一个Boss在一个Tier下的最终运行参数。每个Boss通常配置T1、T2、T3三行。

| 字段 | 类型 | 说明 |
|---|---|---|
| `boss_id` | `INT UNSIGNED` | 关联配置表1 |
| `entry_id` | `INT UNSIGNED` | 新的 `creature_template.entry`；69个Boss统一按 `100001 + (boss_id - 1) * 3 + (tier - 1)` 连续分配 |
| `boss_spawn_x/y/z/o` | `FLOAT` | 现有副本Boss生成点 |
| `tier` | `TINYINT UNSIGNED` | 1、2、3 |
| `health_multiplier` | `DECIMAL(8,4)` | 血量倍率 |
| `damage_multiplier` | `DECIMAL(8,4)` | 伤害倍率 |
| `player_entry_x/y/z/o` | `FLOAT NULL` | 该Tier最终使用的玩家传入点；通常复制表1，允许特殊Tier覆盖；四字段可同时为NULL，但不得部分为空 |

主键为 `boss_id + tier`，`entry_id`必须唯一。

表1的传入点是Boss基础默认值，表2保存Tier最终生效值。运行时优先读取表2；如果表2传入点未填写或校验失败，则该Tier配置无效，不使用任意默认坐标。

### 5.3 配置加载

服务器启动时读取两张表，加载到内存配置缓存：

```text
BossConfig[boss_id]
TierConfig[boss_id][tier]
```

运行期间不应每次进入都依赖临时查询来拼接配置。配置加载失败、Boss缺少某个Tier或Entry不存在时，应在启动日志中报告并将该Boss-Tier排除出随机池。

## 6. 同一Boss只使用一个C++脚本

这是最终版本的关键约束。

### 6.1 脚本绑定规则

同一个Boss的T1、T2、T3虽然使用三个不同的 `entry_id`，但全部绑定同一个该Boss脚本。例如：

```text
死亡矿井范克里夫：
100001 / 100002 / 100003
ScriptName = boss_rift_vancleef

斯尼德：
100004 / 100005 / 100006
ScriptName = boss_rift_sneed
```

不创建以下形式的重复脚本：

```text
boss_rift_vancleef_t1
boss_rift_vancleef_t2
boss_rift_vancleef_t3
```

也不要求所有不同Boss共用一个万能Boss脚本。脚本粒度是“一个Boss一份脚本，三个Tier共用”。

### 6.2 脚本内Tier控制

每个Boss脚本在AI初始化或Boss召唤绑定阶段读取本场内存运行上下文：

```cpp
struct HeroicDungeonRiftTierConfig
{
    uint32 EntryId;
    uint8 Tier;
    float HealthMultiplier;
    float DamageMultiplier;
    Position BossSpawn;
    Position PlayerEntry;
};
```

Boss脚本通过统一的Tier值控制：

```cpp
switch (_riftTier)
{
    case 1:
        ConfigureTier1();
        break;
    case 2:
        ConfigureTier2();
        break;
    case 3:
        ConfigureTier3();
        break;
}
```

实际实现中可以共用同一组事件、计时器和技能函数，只在Tier判断中改变参数：

- `health_multiplier`：初始化Boss生命；
- `damage_multiplier`：普通攻击和技能伤害修正；
- Tier 2：在T1技能基础上启用额外技能或阶段事件；
- Tier 3：在T2技能基础上继续启用更高阶技能或提高新增机制组合密度；
- 原版T1技能的使用时机和CD在全部Tier保持原版，不因Tier缩短；T2/T3新增技能的目标数量、技能伤害、触发阈值和技能冷却由脚本内Tier分支控制，并与其他高伤害技能错峰；
- 不根据队伍人数或NPCBot数量进行二次缩放。

技能增强通过C++运行时逻辑、`CustomSpellValues`、`SpellScript`或脚本伤害修正实现，不修改 `Spell.dbc`。

### 6.3 80级10人本伤害基线

首批裂隙Boss固定使用83级和WotLK属性列：

```text
minlevel/maxlevel = 83/83
exp               = 2
DamageModifier    = 35.0
```

83级提供标准团队Boss的415点近战/防御技能；`DamageModifier=35`取当前数据库中纳克萨玛斯等早期80级10人Boss的常见近战基线。现有表2 `damage_multiplier`继续在Boss最终造成伤害时应用一次，因此等效近战强度约为：

```text
T1：35 × 1.0 = 35
T2：35 × 1.4 = 49
T3：35 × 2.0 = 70
```

等级从20提高到83也会同时改变 `creature_classlevelstats`中的基础生命。**T1 血量按职业分档设置**：`HealthModifier` 按 unit_class 取固定值，血量 = `basehp2(83) × HealthModifier`，随职业的 `basehp2` 自然浮动（战士/圣骑士多、法系少），再由表2 `health_multiplier` 得到 T1/T2/T3 最终生命：

```text
83级模板 HealthModifier（按 unit_class 分档）：
  战士/圣骑士（unit_class 1/2）：HealthModifier = 129 → 血量 = 13945 × 129 ≈ 179.9 万
  法系（unit_class 8）       ：HealthModifier = 115 → 血量 = 11156 × 115 ≈ 128.3 万

T1 = basehp2 × HealthModifier × 1.0   （战士/圣骑士 ≈ 180 万，法系 ≈ 128 万）
T2 = T1 × 1.5
T3 = T1 × 3.0
```

47 个源 Boss 中 unit_class=8（法系）仅「秘法师杜安」1 个，其余 46 个为 class 1/2（战士/圣骑士），故实际只有杜安一档血量为 128 万，其余均为 180 万。

`DamageModifier=35`取纳克萨玛斯 10 人全量 18 个 Boss 的一致近战基线，T1=35、T2=49、T3=70。生命与伤害基线均按固定值正向设置，不随源 Boss 低级属性变化；不再使用“反向折算保持源生命”的旧公式。

召唤物血量按比例设置：以原版召唤物与原版 Boss 的血量比例，乘以新 Boss 当前生命（按职业分档的 `basehp2(83) × HealthModifier`，再乘 Tier 倍率）得到召唤物血量，保持原版的相对强度。82 级召唤物模板 `HealthModifier` 公式：

```text
召唤物 HealthModifier = 新 Boss 生命(basehp2(83)×129 或 ×115)
  × (源召唤物原始生命 ÷ 源 Boss 原始生命)
  ÷ basehp2(82, 对应 unit_class)
```

其中源召唤物/源 Boss 原始生命由 `creature_classlevelstats.basehp` × `HealthModifier` 求得，源 Boss 通过 `_rift_summon_map.boss_source_entry` 关联。运行期 `ApplySummonTierStats` 再按 Tier 与每召唤物系数做最终缩放。

召唤物法术伤害按地下城版本分离基线。现有60级经典裂隙继续使用 `RiftSummonAI` 的15倍低等级法术基础修正，以保持已验证内容的既有行为；70级遭遇的所有专用召唤物统一继承 `RiftLevel70SummonAI`，只应用表2 Tier伤害倍率与该召唤物的 `damageCoefficient`，不再额外乘15。原因是TBC英雄法术本身已有接近目标区间的基础值，例如纳杉火锥 `30926` 约1387–1611、虚灵学徒火球 `32369` 约1147–1551、门努火焰新星 `33132` 约2312–2686；再次统一乘15会造成明显过量。低基础法术如孢子漫游者闪电箭应通过进服结果做逐技能覆盖，不引入新的70级统一倍数。

原技能按实际伤害类型处理：

| Boss | 技能 | 伤害处理 |
|---|---|---|
| 范克里夫 | `3391` Thrash | 无独立伤害基础点，追加2次普通攻击；完全依赖83级近战基线与Tier最终倍率 |
| 范克里夫 | `16856` Mortal Strike | 200%武器伤害；依赖83级近战基线与Tier最终倍率 |
| 范克里夫 | `15589` Whirlwind | 武器伤害外加固定伤害；效果0局部覆盖为2500，再由Tier最终倍率应用一次 |
| 斯尼德 | `7399` Terrify | 只造成恐惧，不改伤害点 |
| 斯尼德 | `3603` Distracting Pain | 只降低施法速度，不改伤害点 |
| 斯尼德 | `15588` Thunderclap | 效果0局部覆盖为2500，再由Tier最终倍率应用一次 |
| 斯尼德 | `10101` Knock Away | 武器伤害外加固定伤害并击退；效果0局部覆盖为2000，再由Tier最终倍率应用一次 |
| 曲奇 | `5174` Cookie's Cooking | 自我治疗，不改伤害点 |
| 曲奇 | `6306` Acid Splash | 每5秒自然伤害，效果0局部覆盖为每跳2000，再由Tier最终倍率应用一次 |
| 曲奇 | `29901` Acidic Fang | 周期自然伤害兼有武器伤害和破甲；效果0局部覆盖为每跳1000，再由Tier最终倍率应用一次 |
| 曲奇 | `23861` Poison Cloud | 每秒周期自然伤害，效果0局部覆盖为每跳1500，再由Tier最终倍率应用一次 |

上述判断已用用户提供的 `outputs/Spell_dbc.csv`复核：`6306`振幅5000毫秒，`29901`振幅2000毫秒且最多叠加7层，`23861`振幅1000毫秒；`3391`确为额外2次攻击机会，`16856`为200%武器伤害，`7399/3603`均无直接伤害效果。由于周期持续时间仍由 `DurationIndex`间接引用，最终持续秒数和叠层压力仍需进服实测。

固定或周期伤害使用 `CastCustomSpell(..., SPELLVALUE_BASE_POINT0, ...)`，只覆盖本裂隙Boss当前这次施法，不修改全服 `Spell.dbc`，也不影响其他使用相同法术ID的生物。`DamageDealt`阶段再统一应用一次Tier倍率，禁止把Tier倍率预乘进上述C++基础点。

70级T2/T3新增技能不再机械继承原版Boss的统一法术倍率。当前分支中这些技能横跨TBC英雄本、WotLK英雄本和10人团队副本，DBC基础伤害差异很大；例如同为“奥术冲击”，不同Spell ID的伤害效果索引也不一定相同。实现必须从 `outputs/Spell_dbc.csv` 逐Spell ID核对 `Effect1/2/3`、`EffectApplyAuraName1/2/3` 与 `EffectBasePoints1/2/3`，再按类型处理：

- 普通单体直接伤害：T1基准通常约4000–5000；
- 带控制或范围伤害：T1基准约3500–5500；
- 高风险长读条范围爆发：T1基准约6500；
- 常规周期伤害：T1基准约1500–2000/跳；
- 分摊技能：按T1全队总量设计，例如音速尖啸以30000为T1基准总量，T3应用2.0倍后约60000总量；
- 武器伤害、百分比伤害、纯控制、治疗、召唤和增益：保留原机制，不机械写入固定法术伤害。

公共接口 `CastFinalRaidDamageSpell(...)` 统一接收T1基准伤害。内部 `GetUnscaledRaidDamage(...)` 只反算该Boss的 `_raidSpellDamageMultiplier`，不再反算 `TierConfig::DamageMultiplier`；随后 `DamageDealt` 统一应用当前Tier倍率，因此调用方禁止再次预乘Tier倍率。最终名义伤害统一为“T1基准 × 当前Tier DamageMultiplier”，即T1为1.0倍、T2为1.4倍、T3为2.0倍。单效果重载必须显式传入正确的 `SPELLVALUE_BASE_POINT0/1/2`；双效果重载固定依次写入BP0和BP1，因此调用参数顺序必须与DBC效果顺序一致。当前70级审计发现并修正了帕萨雷恩 `35314` 的伤害效果实际位于BP1，而非BP0；凯尔萨斯T2 `36834` 的4000也改为T1基准，实际T2约5600、T3约8000。布洛戈克酸息术的1500周期伤害和3000直接伤害是T1基准，T2约2100/跳和4200直伤，T3约3000/跳和6000直伤；其效果顺序仍按DBC的BP0周期、BP1直接传入。毒箭之雨、黑色阔步者的闪电云及其他 `CastFinalRaidDamageSpell(...)` 调用同样遵循这一统一基准语义。

当前10人伤害参考来自本分支纳克萨玛斯脚本所用DBC样本：普通直接伤害中位数约4612.5，中间50%约2643–8437，常规周期伤害每跳中位数约2000。该统计只用于设定起始目标区间，不替代减伤、抗性、目标数量、持续时间、叠层、施法频率和进服实测。

60级47个Boss的T2/T3新增技能也按上述规则完成逐Spell ID复核：普通直接伤害多使用3500（范围/带控制）或4500（单体/常规法术）的T1基准，周期伤害通常使用1800–2000/跳；混合效果只覆盖真实伤害BP，减速、定身、破甲、武器伤害、治疗、召唤和增益仍由原法术机制处理。例如 `21793` 扭曲宁静只覆盖BP0为1800/跳，BP1/BP2的移动及攻击速度降低保持DBC原值；`11972` 盾击保留BP0打断，只覆盖BP1直接伤害为3500 T1基准。

父Aura触发子伤害法术不能使用普通 `CastFinalRaidDamageSpell` 覆盖父法术BP。杜安奥术飞弹 `15790` 的效果0是 `PERIODIC_TRIGGER_SPELL`，每跳真实伤害位于 `15791/BP0`；裂隙专用AuraScript阻止该裂隙Boss的默认周期触发，并由杜安以反算后的1800 T1基准施放 `15791`。`15790` 保持非triggered引导施法，并注册施法期间定向免疫打断，不能为了调伤害改成瞬发。蕾瑟塔蒂丝荆棘诅咒 `16247` 的效果0是 `PROC_TRIGGER_SPELL`，真实反伤位于 `16248/BP0`；专用AuraScript保留原50%触发条件和攻击者目标，阻止默认反伤后由被诅咒者施放自定义 `16248`，并把原始施法者指定为裂隙Boss，使3500 T1基准继续经过Boss的原版15倍反算和Tier倍率路径。两个脚本均先检查Aura施法者是否为有效的T2/T3裂隙Boss；普通世界生物使用同一法术时不接管默认行为。

### 6.4 原版技能CD与新增技能施法规则

60级47个Boss的原版/T1基础技能必须保持统一的原版时间轴：首次施法时间和后续循环CD在T1/T2/T3完全一致，不允许使用 `_tier`、`ScheduleTieredEvent(...)`、`TierDelay(...)` 或Tier配置缩短CD。原版遭遇中的同伴、阶段守卫和召唤物技能同样适用，包括怀特迈恩、鲁兹鲁、阿扎达斯守卫、地缚图腾、傀儡统帅构造体、七贤成员、茉艾拉和克罗卡斯等。实现取当前T1/原版档作为固定值；如果旧代码只有T3与非T3两档，则固定为非T3值。难度差异只通过属性、伤害倍率和明确标记的T2/T3新增技能体现，不改变原版技能使用频率。

T2/T3 新增技能及仅在T2/T3启用的新增机制仍可按难度设置合理CD并与高伤害技能错峰；例如阿库麦尔新增召唤波、暴食者新增疾病脉冲、赫洛德新增周期学员波不受上述原版CD固定规则约束。新增技能按施法时间分档处理：

- **新增的引导施法类型技能必须保持原引导/读条**，不得为了规避打断改成 `triggered=true`。
- **新增引导或明确要求不可打断的读条技能，在施法期间定向免疫打断**：通过 `BossAIBase` 的 `AddInterruptImmuneSpell()` 注册技能 ID，`OnSpellStart` 开始时施加 `SPELL_EFFECT_INTERRUPT_CAST` 免疫、`OnSpellCast` 结束时解除，使 Kick/拳击/法术反制等打断对该次施法无效。
- **原 Boss 技能（T1 基础技能）不改变**，保持与源 Boss 一致（包括读条与可打断性），不做全局免疫打断。
- 非引导型T2/T3新增技能可根据该法术在3.3.5分支中的原始用途选择瞬发或保留读条，但不能用统一“施法时间大于某阈值就改顺发”的规则覆盖具体机制。

当前70级落位包括：迁跃扭木的宁静、玛拉达尔的精神鞭笞、凯尔萨斯的重力失效引导均使用非triggered施法，并注册定向打断免疫。卡利瑟里斯的督军之怒保持原版实现：由可被击杀的蒸馏器对Boss触发法术，不属于Boss自身可打断读条，因此不注册Boss定向打断免疫。以后新设计的Boss应优先选择当前3.3.5分支中已有且机制合适的瞬发技能；若选择引导技能，则必须保留引导并在施法期间免疫打断。不修改 `Spell.dbc`。

### 6.5 脚本命名与目录

源码目录固定为：

```text
src/server/scripts/HeroicDungeonRift/
```

共享代码和管理代码放在根目录；每个副本建立独立子目录，Boss脚本放在对应副本目录中：

```text
HeroicDungeonRift/
├── rift_script_loader.cpp
├── rift_defines.h
├── rift_config.cpp
├── rift_manager.cpp
├── rift_boss_base.h
├── rift_entry.cpp
├── rift_exit_portal.cpp
├── rift_hooks.cpp
└── deadmines/
    ├── boss_rift_vancleef.cpp
    ├── boss_rift_sneed.cpp
    ├── boss_rift_cookie.cpp
    └── rift_deadmines_loader.cpp
```

每个Boss只注册一个脚本名，例如 `boss_rift_vancleef`，由该脚本内部处理全部T1/T2/T3逻辑。

模块总入口：

```cpp
void AddHeroicDungeonRiftScripts();
```

## 7. 分Tier模型、统一逻辑的出口传送门

### 7.1 GameObject模板

T1/T2/T3使用三个连续的出口模板，但共用同一个脚本：

```text
T1：gameobject_entry = 100500，displayId = 7148
T2：gameobject_entry = 100501，displayId = 8196
T3：gameobject_entry = 100502，displayId = 8197
三者：ScriptName = rift_exit_portal
```

当前核心的 `GameObject::SetDisplayId()`确实支持在对象已经进入世界后更换模型，并同步重建客户端字段和服务器动态碰撞模型。但统一Entry运行时换模仍有两个边界：

- `SummonGameObject()`先按模板默认模型创建，再调用 `SetDisplayId()`，附近客户端可能短暂收到默认模型后再更新；
- 部分服务器交互距离和边界计算仍读取 `GetGOInfo()->displayId`，即模板原始模型，而不是运行时模型。

因此最终采用三个模板Entry，使对象创建包、VMAP碰撞和交互边界从生成时起完全一致；不需要修改核心，也不直接写 `GAMEOBJECT_DISPLAYID` 更新字段。

### 7.2 动态生成

实例创建完成、玩家传入点校验通过并完成传送后，在该场实际生效的玩家传入点附近按Tier生成一扇动态出口门：

- 所有副本共用100500/100501/100502这组三档出口模板；
- 当前场次只根据Tier选择其中一个Entry；
- 三个Entry共用同一个 `rift_exit_portal` 对话和离场脚本；
- 每场只生成一扇门，不为每名玩家生成独立门；
- 门GUID、实例ID、Boss ID、Tier和 `runToken` 只保存在内存运行组件；
- 实例卸载、运行结束、超时或回滚时删除动态门。

### 7.3 固定对话与服务器控制

门的对话固定为：

```text
离开...
```

`GameObjectScript`在点击时校验：

1. 玩家当前地图和实例是否属于本场；
2. 玩家是否是本场已进入的真实玩家；
3. 门GUID和 `runToken` 是否有效；
4. 玩家是否已经离场；
5. 是否存在本场共享返回点。

校验通过后，服务器读取本场唯一的共享返回点。这个返回点只在入口对话者发起挑战时记录一次：

```text
入口对话者进入前的地图
入口对话者进入前的实例ID（如果原位置属于实例）
入口对话者进入前的X/Y/Z/O
```

无论是入口对话者、队长还是其他真实队员，只要通过本场出口门选择“离开...”，都传送到上述同一个位置。成员传送成功后只标记该成员已经离场，不删除共享返回点；共享返回点保留到全体成员离开或本场运行上下文清理。客户端不能提交或修改返回坐标。

## 8. 运行态内存模型

```text
HeroicDungeonRiftRunComponent
├── runToken
├── sourceType / sourceGuid
├── initiatorGuid
├── groupGuid / leaderGuid
├── bossId / tier / entryId
├── mapId / instanceId
├── bossSpawnPosition
├── playerEntryPosition
├── sharedReturnLocation
│   ├── returnMapId
│   ├── returnInstanceId
│   └── returnX/Y/Z/O
├── bossGuid
├── exitPortalGuid
├── members[]
│   ├── playerGuid
│   ├── entered
│   ├── exited
│   └── rollbackState
└── encounterState
```

运行态不进入以下数据库表：

- 不新增 `heroic_dungeon_rift_run`；
- 不新增 `heroic_dungeon_rift_run_player`；
- 不将入口对话者的共享返回坐标写入World数据库；
- 不将动态门GUID写入World数据库；
- 不把实例临时状态写入两张静态配置表。

## 9. 实例创建、传入和清理

### 9.1 创建顺序

```text
队长选择T1/T2/T3
    ↓
检查队伍、队长权限和最多5席位
    ↓
只保存入口对话者的共享返回位置
    ↓
随机Boss并读取Tier配置
    ↓
创建对应普通InstanceMap并确认instanceId
    ↓
全体真实玩家预绑定到同一个InstanceSave
    ↓
全体真实玩家进入同一实例
    ↓
在传入点1至2码范围安全散点
    ↓
生成对应Tier Boss
    ↓
按Tier生成对应模型的出口门
    ↓
Boss进入可战斗状态
```

Boss必须使用表2的 `entry_id` 和 `boss_spawn_x/y/z/o`。不使用源Boss的静态世界刷怪，不把T1/T2/T3作为客户端地下城难度分支。

### 9.2 失败回滚

- 随机配置无效：不创建运行态，不传送玩家；
- 队长入本失败：使用入口对话者的共享返回点回滚；
- 队员入本失败：失败成员和需要整体回滚的其他成员均返回同一个共享返回点；推荐本场整体回滚；
- Boss生成失败：删除动态门，所有已传入真实玩家返回入口对话者的共享返回点；
- 运行上下文失效：门不可用并删除，不能继续接受客户端请求。

### 9.3 死亡、释放灵魂和实例销毁

核心判断实例内是否有玩家使用 `Map::HavePlayers()`，本质上检查地图玩家引用容器是否为空，不检查玩家是否存活。因此：

1. 玩家死亡但尚未释放灵魂时，玩家对象仍在实例地图中，实例仍被视为有人，不会启动空图卸载；
2. 玩家释放灵魂后，核心通常把幽灵送到副本外墓地；当最后一名玩家对象离开实例后，`InstanceMap::AfterPlayerUnlinkFromMap()`启动空实例卸载计时；
3. 默认 `Instance.UnloadDelay=1800000`，即30分钟，实例不会在全员释放后立即销毁；
4. 玩家在30分钟内跑尸并重新进入时，临时 `InstanceSave`绑定仍指向原 `instanceId`，进入同一实例，同时 `InstanceMap::AddPlayerToMap()`将卸载计时清零；
5. 若30分钟内无人重新进入，核心在计时结束后调用实例销毁流程；裂隙的 `OnDestroyInstance`回调随后清理Boss、出口门、内存运行态和仍指向该实例的临时玩家绑定；
6. 实例地图对象销毁与 `InstanceSave`删除是两个概念。地图对象先卸载；当没有玩家绑定且地图已不存在时，`InstanceSave`才满足删除条件。

`MemberState::Exited`只表示永久离场。仅以下路径可以设置为真：

- 玩家通过裂隙出口门成功传回共享位置；
- 启动失败或运行失败时，玩家成功回滚到共享位置；
- 裂隙超时回滚完成。

普通地图离开事件不再设置 `Exited=true`，因此释放灵魂、断线或临时离图不会提前触发“全员已经离场”的脚本清理，也不会提前解除跑尸所需的临时绑定。

核心30分钟空图计时与裂隙2小时运行超时的关系是“先到者生效”：

- 全员释放且30分钟无人返回：核心先销毁空实例并触发裂隙清理；
- 实例中一直有人或期间有人返回：核心不销毁，裂隙最多保留至2小时运行超时；
- 2小时先到：裂隙执行回滚和清理，不再继续保留本场。

### 9.4 重复挑战

裂隙玩法本身不设置日锁、周锁、Boss锁定、次数或额外冷却，允许队伍连续反复挑战。

服务器核心的 `AccountInstancesPerHour` 计划调整为50，作为核心滚动小时安全上限。该上限不是裂隙自身的日/周限制。每场结束后必须：

- 让成员安全离开实例；
- 清理本场运行上下文；
- 删除出口门；
- 解除临时实例绑定；
- 下一场创建新的 `instanceId`。

## 10. 数据库与SQL交付边界

所有自定义ID均在SQL顶部定义基数变量，执行语句统一使用 `@变量 + 偏移`。完整编号如下：

```text
入口NPC：100000
全部69个Boss的T1/T2/T3模板：100001–100207
  60级 boss_id 1–47：100001–100141
  70级 boss_id 48–69：100142–100207
全部裂隙专用同伴和召唤物：102000–102058
  60级召唤物：102000–102033
  70级召唤物：102034–102058
T1/T2/T3出口门：100500–100502
```

Boss的三个Tier在同一连续区间内按统一公式分配：

```text
BossEntry(boss_id, tier)
  = 100001 + (boss_id - 1) * 3 + (tier - 1)

T1 = 100001 + (boss_id - 1) * 3
T2 = T1 + 1
T3 = T1 + 2
```

SQL基数与70级衔接偏移为：

```sql
SET @RIFT_BOSS_ID_BASE := 1;
SET @RIFT_BOSS_ENTRY_BASE := 100001;
SET @RIFT_SUMMON_ENTRY_BASE := 102000;
SET @RIFT_LEVEL70_BOSS_ID_OFFSET := 47;
SET @RIFT_LEVEL70_BOSS_ENTRY_OFFSET := 141;
SET @RIFT_LEVEL70_SUMMON_ENTRY_OFFSET := 34;
```

`creature_template.DamageModifier`同样从模板生成语句中提取为变量：

```sql
SET @RIFT_BOSS_DAMAGE_MODIFIER := 35.0000;
SET @RIFT_SUMMON_DAMAGE_MODIFIER := 12.0000;
```

这两个基础模板变量与表2的 `@RIFT_T1_DAMAGE/@RIFT_T2_DAMAGE/@RIFT_T3_DAMAGE` 运行时Tier倍率相互独立。

三档Entry复制源Boss的必要模板数据：

- `creature_template`基础属性；
- `creature_template_model`模型；
- `creature_template_spell`基础技能；
- `creature_template_resistance`抗性；
- 必要的 `creature_template_addon`。

覆盖字段：

- `entry`改为新Entry；
- `difficulty_entry_1/2/3`置0；
- `minlevel/maxlevel=83/83`、`exp=2`，获得标准80级团队Boss的属性和415点近战技能；
- `DamageModifier=35.0`作为早期80级10人本近战基线，表2伤害倍率只由C++在运行时应用；
- `HealthModifier`按职业分档设置：战士/圣骑士（unit_class 1/2）取 `129`（血量≈180万）、法系（unit_class 8）取 `115`（血量≈128万），血量=basehp2(83)×HealthModifier；表2生命倍率（1.0/1.5/3.0）只由C++应用；
- `ScriptName`设置为该Boss唯一脚本名，例如 `boss_rift_vancleef`；
- Boss通常清除不适用的NPC标志和巡逻路径；
- T1/T2/T3如需独立掉落，后续分别配置对应掉落Entry。

不使用 `INSERT ... SELECT *`，应使用显式列名，防止核心字段变化导致错位。SQL只生成并审核，不自动执行。

SQL提供两种互斥的审核与安装入口：

```text
outputs/五人英雄裂隙全部副本整合SQL（暂不执行）.sql
  - 一次性安装60级与70级全部69个遭遇

outputs/70级五人英雄裂隙扩展SQL（暂不执行）.sql
  - 已经安装60级基础内容后，仅追加boss_id 48–69
```

两份SQL使用完全相同的全局基数；70级扩展文件只包含Boss Entry 100142–100207与召唤物Entry 102034–102058，不重新建立编号体系。两份文件不得对同一数据库重复叠加执行。早期的死亡矿井首批SQL和简化配置SQL均已废弃，不得与当前文件同时执行。

## 11. 明确不采用的方案

- 不为同一个Boss的T1/T2/T3分别创建三份C++脚本；
- 不让同一Boss的不同Tier使用不同 `ScriptName`；
- 不使用一个覆盖所有Boss的万能脚本代替“每个Boss一份、三个Tier共用”的脚本结构；
- 不按玩家或Bot数量动态调整Boss强度；
- 不伪装LFG队伍；
- 不把运行态写入数据库；
- 不使用全局Recall点作为共享返回位置；
- 不记录每个队员各自的离场返回点，只记录入口对话者的位置；
- 不为每个玩家生成一扇出口门；
- 不将共享返回坐标交给客户端控制；
- 不修改 `Spell.dbc`。

## 12. 当前实现状态

已在 `src/server/scripts/HeroicDungeonRift/` 创建独立脚本模块。经典旧世部分包含47个裂隙遭遇；本次继续加入燃烧的远征14张副本地图、22个70级裂隙遭遇和66个Boss Tier模板。以下为首批16个遭遇示例：

| 地图 | 裂隙遭遇 |
|---|---|
| 死亡矿井 | 艾德温·范克里夫、斯尼德的伐木机、曲奇 |
| 诺莫瑞根 | 电刑器6000型、机械师瑟玛普拉格 |
| 剃刀高地 | 寒冰之王亚门纳尔、暴食者 |
| 剃刀沼泽 | 主宰拉姆塔斯、卡尔加·刺肋 |
| 血色修道院 | 莫格莱尼/怀特迈恩双Boss、赫洛德 |
| 黑暗深渊 | 暮光领主克尔里斯、阿库麦尔 |
| 奥达曼 | 阿扎达斯 |
| 沉没的神庙 | 伊兰尼库斯的阴影、预言者迦玛兰 |

莫格莱尼/怀特迈恩按一个遭遇配置计算，因此共16个Boss配置；双Boss中的怀特迈恩使用裂隙专用同伴Entry。已实现：

- 两张World静态配置表的加载缓存；
- 队长发起、1至5名真实玩家校验、先随机副本再随机Boss；
- 普通副本新实例创建和整队临时绑定，避免成员分流到不同实例；
- 仅清除本场对应的原版Boss，不清除原版小怪；
- 每个Boss各自一份脚本，同一Boss的T1/T2/T3共用该脚本；
- 莫格莱尼/怀特迈恩使用动态GUID协作的双Boss状态机，莫格莱尼首次致死进入假死，怀特迈恩50%触发睡眠和复活，复活后的两名Boss进入可真正死亡的第二阶段；
- 瑟玛普拉格步行炸弹、亚门纳尔冰霜亡魂、赫洛德预备兵、迦玛兰图腾、阿扎达斯四类守卫和阿库麦尔两类小怪使用裂隙专用Entry，并按Tier增强；
- 公共Boss基类统一记录和清理裂隙召唤物，Reset、Boss死亡和运行清理均不会把专用召唤物遗留在实例中；
- C++运行时统一应用生命和伤害倍率；Boss普通近战只应用Tier伤害倍率，低级伤害法术按逐Boss DBC审查结果应用局部倍率；召唤物普通武器伤害在实例属性上缩放，法术伤害在其AI回调中缩放，二者不重复作用于同一种伤害；60级召唤物保留既有15倍低级法术基线，70级召唤物使用独立基类并取消该额外15倍，只应用Tier倍率和召唤物系数；
- 实例内按Tier动态生成模型7148/8196/8197对应的出口模板，三档共用固定“离开...”选项和同一脚本；
- 记录入口对话者共享返回点，并支持返回其原副本实例；
- 传送启动失败保留短暂回滚上下文，防止异步晚到成员落入已经清理的孤立实例；
- 两小时超时和失败回滚先将仍在裂隙内的成员送回共享返回点，再清理动态实体、内存上下文和裂隙临时绑定；
- 释放灵魂、断线或普通离图不再被标记为永久退出，保留运行态和临时绑定供跑尸返回同一实例；核心真正卸载实例时再解除剩余裂隙绑定；
- 83级/exp=2和早期80级10人本近战基线，武器类技能跟随近战伤害缩放，固定/周期伤害技能使用当前裂隙施法的局部基础点覆盖；
- 真实玩家1至2码散点在目标实例地图中重新查询地面高度，无有效高度时才保留配置Z。

70级新增遭遇如下；其基础表 `dungeon_version=70`，基础表与Tier表的玩家传入点均暂留NULL，并默认 `enabled=0`。候选传入点只保留在审核表格，地图实测通过后再回填Tier表。除维斯路登外，Boss刷新点均来自原版 `creature` 表；维斯路登使用原版脚本召唤点。

| 副本 | 70级裂隙Boss |
|---|---|
| 破碎大厅 | 高阶术士奈瑟库斯、酋长卡加斯·刃拳 |
| 鲜血熔炉 | 布洛戈克、击碎者克里丹 |
| 地狱火城墙 | 无疤者奥摩尔、维斯路登/纳杉 |
| 蒸汽地窟 | 督军卡利瑟里斯 |
| 幽暗沼泽 | 沼地领主穆塞雷克、黑色阔步者 |
| 奴隶围栏 | 背叛者门努、夸格米拉 |
| 禁魔监狱 | 末日预言者达尔莉安、天怒预言者苏克拉底 |
| 生态船 | 拉伊、迁跃扭木 |
| 能源舰 | 计算者帕萨雷恩 |
| 暗影迷宫 | 摩摩尔 |
| 塞泰克大厅 | 利爪之王艾吉斯 |
| 法力陵墓 | 节点亲王沙法尔 |
| 奥金尼地穴 | 死亡观察者希尔拉克、大主教玛拉达尔 |
| 魔导师平台 | 凯尔萨斯·逐日者 |

当前应根据数据库状态二选一审核SQL：

```text
outputs/五人英雄裂隙全部副本整合SQL（暂不执行）.sql
outputs/70级五人英雄裂隙扩展SQL（暂不执行）.sql
```

两份SQL均尚未执行。入口NPC模板已分配 `creature_template.entry=100000`，来源模板为8379（Archmage Xylem，模型7591）；入口NPC的世界生成地图和坐标尚未确定，因此SQL不插入 `creature` 世界生成行。经典旧世47个遭遇和70级22个遭遇共生成69组、207个Tier模板，统一连续使用Entry 100001–100207；其中70级模板为100142–100207。59个裂隙召唤物统一使用102000–102058，其中70级为102034–102058。70级玩家传入点候选值只保留在 `outputs/英雄裂隙boss传入点_整合.xlsx`，正式SQL的基础表和Tier表在地图实测通过前均保持NULL，且基础配置 `enabled=0`。出口门按Tier使用Entry 100500/100501/100502，模型分别为7148/8196/8197，三个模板只从现有Portal 181229复制交互类型、Data字段和尺寸，并统一绑定 `rift_exit_portal`。

Boss传入坐标以 `outputs/boss传入点.xls` 为准；斯尼德和曲奇保留此前建议点。全部坐标导入前仍必须进服实测地图高度、碰撞、巡逻、仇恨范围和可达性。死亡矿井三个示例如下：

| Boss | 建议传入点 `X / Y / Z / O` | 主要实测风险 |
|---|---|---|
| 范克里夫 | `6.500 / -761.146 / 9.630 / 3.900` | 表格坐标；船体上下层跨层引怪 |
| 斯尼德的伐木机 | `-309.000 / -520.000 / 49.400 / 0.343` | 周边小怪巡逻和仇恨 |
| 曲奇 | `-67.600 / -838.000 / 17.100 / 4.713` | 船舱高度、碰撞和可达性 |

首批额外Tier技能已从当前3.3.5分支的现有法术用法中选择：

| Boss | T2新增 | T3新增 |
|---|---|---|
| 范克里夫 | `16856` Mortal Strike，当前目标 | `15589` Whirlwind，当前目标 |
| 斯尼德的伐木机 | `15588` Thunderclap，当前目标 | `10101` Knock Away，当前目标 |
| 曲奇 | `29901` Acidic Fang，当前目标 | `23861` Poison Cloud，自身 |

此前经典裂隙内容已使用 Visual Studio 18/2026 Developer PowerShell、MSVC、C++20 和 Ninja 完成真实构建验证：`HeroicDungeonRift`进入静态脚本集合，`scripts`目标完整编译并成功链接 `worldserver.exe`。本次新增的22个70级遭遇及其后续时序、引导免疫、召唤物版本分流和倍率修正仅完成静态审查，尚未执行新的完整构建或增量链接验证；因此不能沿用此前构建结果宣称70级改动已编译通过。

本轮静态一致性结果：22个70级Boss文件、47个注册AI和SQL中的47个 `ScriptName` 完全对应；14个区域loader包含22个Boss注册调用，且都已接入顶层loader；207条Boss Tier覆盖69个Boss且每个Boss均有T1/T2/T3。新验证器已确认完整SQL的Boss Entry 100001–100207和59个召唤物Entry 102000–102058连续无缺口；70级扩展SQL只包含boss_id 48–69、Boss Entry 100142–100207和召唤物102034–102058；两份SQL映射一致。SQL中的59个召唤物Entry、AI定义与注册已和C++一一核对，源码无旧召唤物Entry残留。70级66条Tier行的玩家传入点全部为NULL，基础配置 `dungeon_version=70` 且 `enabled=0`。70级43个T2/T3新增技能的Spell ID均存在于 `Spell_dbc.csv`；直接/周期伤害已逐项核对效果索引，武器、控制、治疗、召唤和增益技能保持原机制。`CustomSpellValues`、`TriggerCastFlags`、AI法术回调签名和 `DamageDealt` 签名已与当前分支接口静态核对。上述结果仍属于静态验证，不替代编译和进服测试。

SQL仍未执行，入口NPC世界生成地图与坐标尚未配置，70级玩家传入点在正式SQL中保持NULL且对应基础配置禁用。全部Boss传入点、技能伤害、控制效果、召唤物强度和并存上限仍需在服务端实测；尤其需要复核取消统一15倍后的70级低基础召唤物法术、卡加斯刃舞45倍局部倍率、希尔拉克暗影技能、凯尔萨斯炎爆/重力阶段、维斯路登与纳杉双实体清理及胜利判定，以及跑尸重进、30分钟空实例卸载、整队同实例、NPCBot自动跟随、Free Bot不进入、共享返回原实例和失败回滚。
